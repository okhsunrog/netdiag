// This file was generated by jbindgen. Do not edit manually.

pub mod android {
pub mod net {
    use jni::bind_java_type;

    bind_java_type! {
        /// Identifies a {@code Network}.  This is supplied to applications via
        /// {@link android.net.ConnectivityManager.NetworkCallback ConnectivityManager.NetworkCallback} in response to the active
        /// {@link android.net.ConnectivityManager#requestNetwork ConnectivityManager#requestNetwork} or passive
        /// {@link android.net.ConnectivityManager#registerNetworkCallback ConnectivityManager#registerNetworkCallback} calls.
        /// It is used to direct traffic to the given {@code Network}, either on a {@link java.net.Socket Socket} basis
        /// through a targeted {@link javax.net.SocketFactory SocketFactory} or process-wide via
        /// {@link android.net.ConnectivityManager#bindProcessToNetwork ConnectivityManager#bindProcessToNetwork}.
        pub Network => "android.net.Network",
        type_map = {
            crate::android::net::Network => "android.net.Network",
            crate::java::net::InetAddress => "java.net.InetAddress",
        },
        fields {
            #[allow(non_snake_case)]
            static CREATOR {
                sig = "android.os.Parcelable$Creator",
                get = CREATOR,
            },
        },
        methods {
            /// Binds the specified {@link java.io.FileDescriptor FileDescriptor} to this {@code Network}. All data traffic on the
            /// socket represented by this file descriptor will be sent on this {@code Network},
            /// irrespective of any process-wide network binding set by
            /// {@link android.net.ConnectivityManager#bindProcessToNetwork ConnectivityManager#bindProcessToNetwork}. The socket must not be connected.
            fn bind_socket_file_descriptor {
                name = "bindSocket",
                sig = (fd: "java.io.FileDescriptor"),
            },
            /// Binds the specified {@link java.net.DatagramSocket DatagramSocket} to this {@code Network}. All data traffic on the
            /// socket will be sent on this {@code Network}, irrespective of any process-wide network binding
            /// set by {@link android.net.ConnectivityManager#bindProcessToNetwork ConnectivityManager#bindProcessToNetwork}. The socket must not be
            /// connected.
            fn bind_socket_datagram_socket {
                name = "bindSocket",
                sig = (socket: "java.net.DatagramSocket"),
            },
            /// Binds the specified {@link java.net.Socket Socket} to this {@code Network}. All data traffic on the socket
            /// will be sent on this {@code Network}, irrespective of any process-wide network binding set by
            /// {@link android.net.ConnectivityManager#bindProcessToNetwork ConnectivityManager#bindProcessToNetwork}. The socket must not be connected.
            fn bind_socket_socket {
                name = "bindSocket",
                sig = (socket: "java.net.Socket"),
            },
            fn describe_contents() -> jint,
            fn equals(obj: JObject) -> jboolean,
            /// Returns a {@link android.net.Network Network} object given a handle returned from {@link #getNetworkHandle}.
            ///
            ///
            /// # Parameters
            /// * `network_handle` - a handle returned from {@link #getNetworkHandle}.
            ///
            /// # Returns
            /// A {@link android.net.Network Network} object derived from {@code networkHandle}.
            static fn from_network_handle(network_handle: jlong) -> crate::android::net::Network,
            /// Operates the same as {@code InetAddress.getAllByName} except that host
            /// resolution is done on this network.
            ///
            ///
            /// # Parameters
            /// * `host` - the hostname or literal IP string to be resolved.
            ///
            /// # Returns
            /// the array of addresses associated with the specified host.
            fn get_all_by_name(host: JString) -> crate::java::net::InetAddress[],
            /// Operates the same as {@code InetAddress.getByName} except that host
            /// resolution is done on this network.
            ///
            ///
            /// # Parameters
            /// * `host` - the hostname to be resolved to an address or {@code null}.
            ///
            /// # Returns
            /// the {@code InetAddress} instance representing the host.
            /// if the address lookup fails.
            fn get_by_name(host: JString) -> crate::java::net::InetAddress,
            /// Returns a handle representing this {@code Network}, for use with the NDK API.
            fn get_network_handle() -> jlong,
            /// Returns a {@link javax.net.SocketFactory SocketFactory} bound to this network.  Any {@link java.net.Socket Socket} created by
            /// this factory will have its traffic sent over this {@code Network}.  Note that if this
            /// {@code Network} ever disconnects, this factory and any {@link java.net.Socket Socket} it produced in the
            /// past or future will cease to work.
            ///
            ///
            /// # Returns
            /// a {@link javax.net.SocketFactory SocketFactory} which produces {@link java.net.Socket Socket} instances bound to this
            /// {@code Network}.
            fn get_socket_factory() -> "javax.net.SocketFactory",
            fn hash_code() -> jint,
            /// Opens the specified {@link java.net.URL URL} on this {@code Network}, such that all traffic will be sent
            /// on this Network. The URL protocol must be {@code HTTP} or {@code HTTPS}.
            ///
            ///
            /// # Returns
            /// a {@code URLConnection} to the resource referred to by this URL.
            fn open_connection {
                name = "openConnection",
                sig = (url: "java.net.URL") -> "java.net.URLConnection",
            },
            /// Opens the specified {@link java.net.URL URL} on this {@code Network}, such that all traffic will be sent
            /// on this Network. The URL protocol must be {@code HTTP} or {@code HTTPS}.
            ///
            ///
            /// # Parameters
            /// * `proxy` - the proxy through which the connection will be established.
            ///
            /// # Returns
            /// a {@code URLConnection} to the resource referred to by this URL.
            #[allow(non_snake_case)]
            fn open_connection2 {
                name = "openConnection",
                sig = (url: "java.net.URL", proxy: "java.net.Proxy") -> "java.net.URLConnection",
            },
            fn to_string() -> JString,
            fn write_to_parcel(dest: "android.os.Parcel", flags: jint),
        },
    }

    bind_java_type! {
        /// Describes the properties of a network link.
        ///
        /// A link represents a connection to a network.
        /// It may have multiple addresses and multiple gateways,
        /// multiple dns servers but only one http proxy and one
        /// network interface.
        ///
        /// Note that this is just a holder of data.  Modifying it
        /// does not affect live networks.
        pub LinkProperties => "android.net.LinkProperties",
        constructors {
            /// Constructs a new {@code LinkProperties} with default values.
            fn new(),
        },
        fields {
            /// Implement the Parcelable interface.
            #[allow(non_snake_case)]
            static CREATOR {
                sig = "android.os.Parcelable$Creator",
                get = CREATOR,
            },
        },
        methods {
            /// Adds a {@link android.net.RouteInfo RouteInfo} to this {@code LinkProperties}. If there is a {@link android.net.RouteInfo RouteInfo}
            /// with the same destination, gateway and interface with different properties
            /// (e.g., different MTU), it will be updated. If the {@link android.net.RouteInfo RouteInfo} had an
            /// interface name set and that differs from the interface set for this
            /// {@code LinkProperties} an {@link java.lang.IllegalArgumentException IllegalArgumentException} will be thrown.
            /// The proper course is to add either un-named or properly named {@link android.net.RouteInfo RouteInfo}.
            ///
            ///
            /// # Parameters
            /// * `route` - A {@link android.net.RouteInfo RouteInfo} to add to this object.
            ///
            /// # Returns
            /// {@code true} was added or updated, false otherwise.
            fn add_route(route: "android.net.RouteInfo") -> jboolean,
            /// Clears this object to its initial state.
            fn clear(),
            /// Implement the Parcelable interface
            fn describe_contents() -> jint,
            /// Compares this {@code LinkProperties} instance against the target
            /// LinkProperties in {@code obj}. Two LinkPropertieses are equal if
            /// all their fields are equal in values.
            ///
            /// For collection fields, such as mDnses, containsAll() is used to check
            /// if two collections contains the same elements, independent of order.
            /// There are two thoughts regarding containsAll()
            /// 1. Duplicated elements. eg, (A, B, B) and (A, A, B) are equal.
            /// 2. Worst case performance is O(n^2).
            ///
            ///
            /// # Parameters
            /// * `obj` - the object to be tested for equality.
            ///
            /// # Returns
            /// {@code true} if both objects are equal, {@code false} otherwise.
            fn equals(obj: JObject) -> jboolean,
            /// Get DHCP server address
            ///
            ///
            /// # Returns
            /// The current DHCP server address.
            fn get_dhcp_server_address() -> "java.net.Inet4Address",
            /// Returns all the {@link java.net.InetAddress InetAddress} for DNS servers on this link.
            ///
            ///
            /// # Returns
            /// An unmodifiable {@link java.util.List List} of {@link java.net.InetAddress InetAddress} for DNS servers on
            /// this link.
            fn get_dns_servers() -> JList,
            /// Get the DNS domains search path set for this link. May be {@code null} if not set.
            ///
            ///
            /// # Returns
            /// A {@link java.lang.String String} containing the comma separated domains to search when resolving host
            /// names on this link or {@code null}.
            fn get_domains() -> JString,
            /// Gets the recommended {@link android.net.ProxyInfo ProxyInfo} (or {@code null}) set on this link.
            ///
            ///
            /// # Returns
            /// The {@link android.net.ProxyInfo ProxyInfo} set on this link or {@code null}.
            fn get_http_proxy() -> "android.net.ProxyInfo",
            /// Gets the interface name for this link.  May be {@code null} if not set.
            ///
            ///
            /// # Returns
            /// The interface name set for this link or {@code null}.
            fn get_interface_name() -> JString,
            /// Returns all the {@link android.net.LinkAddress LinkAddress} on this link.  Typically a link will have
            /// one IPv4 address and one or more IPv6 addresses.
            ///
            ///
            /// # Returns
            /// An unmodifiable {@link java.util.List List} of {@link android.net.LinkAddress LinkAddress} for this link.
            fn get_link_addresses() -> JList,
            /// Gets any non-default MTU size set for this link.  Note that if the default is being used
            /// this will return 0.
            ///
            ///
            /// # Returns
            /// The mtu value set for this link.
            fn get_mtu() -> jint,
            /// Returns the NAT64 prefix in use on this link, if any.
            ///
            ///
            /// # Returns
            /// the NAT64 prefix or {@code null}.
            #[allow(non_snake_case)]
            fn get_nat64prefix {
                name = "getNat64Prefix",
                sig = () -> "android.net.IpPrefix",
            },
            /// Returns the private DNS server name that is in use. If not {@code null},
            /// private DNS is in strict mode. In this mode, applications should ensure
            /// that all DNS queries are encrypted and sent to this hostname and that
            /// queries are only sent if the hostname's certificate is valid. If
            /// {@code null} and {@link #isPrivateDnsActive} is {@code true}, private
            /// DNS is in opportunistic mode, and applications should ensure that DNS
            /// queries are encrypted and sent to a DNS server returned by
            /// {@link #getDnsServers}. System DNS will handle each of these cases
            /// correctly, but applications implementing their own DNS lookups must make
            /// sure to follow these requirements.
            ///
            ///
            /// # Returns
            /// The private DNS server name.
            fn get_private_dns_server_name() -> JString,
            /// Returns all the {@link android.net.RouteInfo RouteInfo} set on this link.
            ///
            ///
            /// # Returns
            /// An unmodifiable {@link java.util.List List} of {@link android.net.RouteInfo RouteInfo} for this link.
            fn get_routes() -> JList,
            /// Generate hashcode based on significant fields
            ///
            /// Equal objects must produce the same hash code, while unequal objects
            /// may have the same hash codes.
            fn hash_code() -> jint,
            /// Returns whether private DNS is currently in use on this network. When
            /// private DNS is in use, applications must not send unencrypted DNS
            /// queries as doing so could reveal private user information. Furthermore,
            /// if private DNS is in use and {@link #getPrivateDnsServerName} is not
            /// {@code null}, DNS queries must be sent to the specified DNS server.
            ///
            ///
            /// # Returns
            /// {@code true} if private DNS is in use, {@code false} otherwise.
            fn is_private_dns_active() -> jboolean,
            /// Returns whether the network interface supports WakeOnLAN
            ///
            ///
            /// # Returns
            /// {@code true} if interface supports WakeOnLAN, {@code false} otherwise.
            fn is_wake_on_lan_supported() -> jboolean,
            /// Set DHCP server address.
            ///
            ///
            /// # Parameters
            /// * `server_address` - the server address to set.
            fn set_dhcp_server_address(server_address: "java.net.Inet4Address"),
            /// Replaces the DNS servers in this {@code LinkProperties} with
            /// the given {@link java.util.Collection Collection} of {@link java.net.InetAddress InetAddress} objects.
            ///
            ///
            /// # Parameters
            /// * `dns_servers` - The {@link java.util.Collection Collection} of DNS servers to set in this object.
            fn set_dns_servers(dns_servers: JCollection),
            /// Sets the DNS domain search path used on this link.
            ///
            ///
            /// # Parameters
            /// * `domains` - A {@link java.lang.String String} listing in priority order the comma separated
            /// domains to search when resolving host names on this link.
            fn set_domains(domains: JString),
            /// Sets the recommended {@link android.net.ProxyInfo ProxyInfo} to use on this link, or {@code null} for none.
            /// Note that Http Proxies are only a hint - the system recommends their use, but it does
            /// not enforce it and applications may ignore them.
            ///
            ///
            /// # Parameters
            /// * `proxy` - A {@link android.net.ProxyInfo ProxyInfo} defining the HTTP Proxy to use on this link.
            fn set_http_proxy(proxy: "android.net.ProxyInfo"),
            /// Sets the interface name for this link.  All {@link android.net.RouteInfo RouteInfo} already set for this
            /// will have their interface changed to match this new value.
            ///
            ///
            /// # Parameters
            /// * `iface` - The name of the network interface used for this link.
            fn set_interface_name(iface: JString),
            /// Replaces the {@link android.net.LinkAddress LinkAddress} in this {@code LinkProperties} with
            /// the given {@link java.util.Collection Collection} of {@link android.net.LinkAddress LinkAddress}.
            ///
            ///
            /// # Parameters
            /// * `addresses` - The {@link java.util.Collection Collection} of {@link android.net.LinkAddress LinkAddress} to set in this
            /// object.
            fn set_link_addresses(addresses: JCollection),
            /// Sets the Maximum Transmission Unit size to use on this link.  This should not be used
            /// unless the system default (1500) is incorrect.  Values less than 68 or greater than
            /// 10000 will be ignored.
            ///
            ///
            /// # Parameters
            /// * `mtu` - The MTU to use for this link.
            fn set_mtu(mtu: jint),
            /// Sets the NAT64 prefix in use on this link.
            ///
            /// Currently, only 96-bit prefixes (i.e., where the 32-bit IPv4 address is at the end of the
            /// 128-bit IPv6 address) are supported or {@code null} for no prefix.
            ///
            ///
            /// # Parameters
            /// * `prefix` - the NAT64 prefix.
            #[allow(non_snake_case)]
            fn set_nat64prefix {
                name = "setNat64Prefix",
                sig = (prefix: "android.net.IpPrefix"),
            },
            fn to_string() -> JString,
            /// Implement the Parcelable interface.
            fn write_to_parcel(dest: "android.os.Parcel", flags: jint),
        },
    }

    bind_java_type! {
        /// Class that answers queries about the state of network connectivity. It also
        /// notifies applications when network connectivity changes.
        /// 
        /// The primary responsibilities of this class are to:
        /// <ol>
        /// <li>Monitor network connections (Wi-Fi, GPRS, UMTS, etc.)</li>
        /// <li>Send broadcast intents when network connectivity changes</li>
        /// <li>Attempt to "fail over" to another network when connectivity to a network
        /// is lost</li>
        /// <li>Provide an API that allows applications to query the coarse-grained or fine-grained
        /// state of the available networks</li>
        /// <li>Provide an API that allows applications to request and select networks for their data
        /// traffic</li>
        /// </ol>
        pub ConnectivityManager => "android.net.ConnectivityManager",
        type_map = {
            crate::android::net::ConnectivityManagerNetworkCallback => "android.net.ConnectivityManager$NetworkCallback",
            crate::android::net::ConnectivityManagerOnNetworkActiveListener => "android.net.ConnectivityManager$OnNetworkActiveListener",
            crate::android::net::LinkProperties => "android.net.LinkProperties",
            crate::android::net::Network => "android.net.Network",
            crate::android::net::NetworkCapabilities => "android.net.NetworkCapabilities",
            crate::java::net::InetAddress => "java.net.InetAddress",
        },
        fields {
            /// Broadcast Action: The setting for background data usage has changed
            /// values. Use {@link #getBackgroundDataSetting()} to get the current value.
            /// 
            /// If an application uses the network in the background, it should listen
            /// for this broadcast and stop using the background data if the value is
            /// {@code false}.
            /// 
            ///
            /// of background data depends on several combined factors, and
            /// this broadcast is no longer sent. Instead, when background
            /// data is unavailable, {@link #getActiveNetworkInfo()} will now
            /// appear disconnected. During first boot after a platform
            /// upgrade, this broadcast will be sent once if
            /// {@link #getBackgroundDataSetting()} was {@code false} before
            /// the upgrade.
            #[deprecated]
            #[allow(non_snake_case)]
            static ACTION_BACKGROUND_DATA_SETTING_CHANGED {
                sig = JString,
                get = ACTION_BACKGROUND_DATA_SETTING_CHANGED,
            },
            /// The device has connected to a network that has presented a captive
            /// portal, which is blocking Internet connectivity. The user was presented
            /// with a notification that network sign in is required,
            /// and the user invoked the notification's action indicating they
            /// desire to sign in to the network. Apps handling this activity should
            /// facilitate signing in to the network. This action includes a
            /// {@link android.net.Network Network} typed extra called {@link #EXTRA_NETWORK} that represents
            /// the network presenting the captive portal; all communication with the
            /// captive portal must be done using this {@code Network} object.
            /// <p/>
            /// This activity includes a {@link android.net.CaptivePortal CaptivePortal} extra named
            /// {@link #EXTRA_CAPTIVE_PORTAL} that can be used to indicate different
            /// outcomes of the captive portal sign in to the system:
            /// <ul>
            /// <li> When the app handling this action believes the user has signed in to
            /// the network and the captive portal has been dismissed, the app should
            /// call {@link android.net.CaptivePortal#reportCaptivePortalDismissed CaptivePortal#reportCaptivePortalDismissed} so the system can
            /// reevaluate the network. If reevaluation finds the network no longer
            /// subject to a captive portal, the network may become the default active
            /// data network.</li>
            /// <li> When the app handling this action believes the user explicitly wants
            /// to ignore the captive portal and the network, the app should call
            /// {@link android.net.CaptivePortal#ignoreNetwork CaptivePortal#ignoreNetwork}. </li>
            /// </ul>
            #[allow(non_snake_case)]
            static ACTION_CAPTIVE_PORTAL_SIGN_IN {
                sig = JString,
                get = ACTION_CAPTIVE_PORTAL_SIGN_IN,
            },
            /// A change in the background metered network activity restriction has occurred.
            /// 
            /// Applications should call {@link #getRestrictBackgroundStatus()} to check if the restriction
            /// applies to them.
            /// 
            /// This is only sent to registered receivers, not manifest receivers.
            #[allow(non_snake_case)]
            static ACTION_RESTRICT_BACKGROUND_CHANGED {
                sig = JString,
                get = ACTION_RESTRICT_BACKGROUND_CHANGED,
            },
            /// A change in network connectivity has occurred. A default connection has either
            /// been established or lost. The NetworkInfo for the affected network is
            /// sent as an extra; it should be consulted to see what kind of
            /// connectivity event occurred.
            /// <p/>
            /// Apps targeting Android 7.0 (API level 24) and higher do not receive this
            /// broadcast if they declare the broadcast receiver in their manifest. Apps
            /// will still receive broadcasts if they register their
            /// {@link android.content.BroadcastReceiver} with
            /// {@link android.content.Context#registerReceiver Context.registerReceiver()}
            /// and that context is still valid.
            /// <p/>
            /// If this is a connection that was the result of failing over from a
            /// disconnected network, then the FAILOVER_CONNECTION boolean extra is
            /// set to true.
            /// <p/>
            /// For a loss of connectivity, if the connectivity manager is attempting
            /// to connect (or has already connected) to another network, the
            /// NetworkInfo for the new network is also passed as an extra. This lets
            /// any receivers of the broadcast know that they should not necessarily
            /// tell the user that no data traffic will be possible. Instead, the
            /// receiver should expect another broadcast soon, indicating either that
            /// the failover attempt succeeded (and so there is still overall data
            /// connectivity), or that the failover attempt failed, meaning that all
            /// connectivity has been lost.
            /// <p/>
            /// For a disconnect event, the boolean extra EXTRA_NO_CONNECTIVITY
            /// is set to {@code true} if there are no connected networks at all.
            ///
            /// {@link #registerNetworkCallback} or {@link #registerDefaultNetworkCallback}
            /// functions instead for faster and more detailed updates about the network
            /// changes they care about.
            #[deprecated]
            #[allow(non_snake_case)]
            static CONNECTIVITY_ACTION {
                sig = JString,
                get = CONNECTIVITY_ACTION,
            },
            /// If you want to set the default network preference,you can directly
            /// change the networkAttributes array in framework's config.xml.
            ///
            /// network default network preference can't really express
            /// the hierarchy.  Instead, the default is defined by the
            /// networkAttributes in config.xml.  You can determine
            /// the current value by calling {@link #getNetworkPreference()}
            /// from an App.
            #[deprecated]
            #[allow(non_snake_case)]
            static DEFAULT_NETWORK_PREFERENCE {
                sig = jint,
                get = DEFAULT_NETWORK_PREFERENCE,
            },
            /// The lookup key for a {@link android.net.CaptivePortal CaptivePortal} object included with the
            /// {@link #ACTION_CAPTIVE_PORTAL_SIGN_IN} intent.  The {@code CaptivePortal}
            /// object can be used to either indicate to the system that the captive
            /// portal has been dismissed or that the user does not want to pursue
            /// signing in to captive portal.  Retrieve it with
            /// {@link android.content.Intent#getParcelableExtra(String)}.
            #[allow(non_snake_case)]
            static EXTRA_CAPTIVE_PORTAL {
                sig = JString,
                get = EXTRA_CAPTIVE_PORTAL,
            },
            /// Key for passing a URL to the captive portal login activity.
            #[allow(non_snake_case)]
            static EXTRA_CAPTIVE_PORTAL_URL {
                sig = JString,
                get = EXTRA_CAPTIVE_PORTAL_URL,
            },
            /// The lookup key for a string that provides optionally supplied
            /// extra information about the network state. The information
            /// may be passed up from the lower networking layers, and its
            /// meaning may be specific to a particular network type. Retrieve
            /// it with {@link android.content.Intent#getStringExtra(String)}.
            ///
            #[deprecated]
            #[allow(non_snake_case)]
            static EXTRA_EXTRA_INFO {
                sig = JString,
                get = EXTRA_EXTRA_INFO,
            },
            /// The lookup key for a boolean that indicates whether a connect event
            /// is for a network to which the connectivity manager was failing over
            /// following a disconnect on another network.
            /// Retrieve it with {@link android.content.Intent#getBooleanExtra(String,boolean)}.
            ///
            #[deprecated]
            #[allow(non_snake_case)]
            static EXTRA_IS_FAILOVER {
                sig = JString,
                get = EXTRA_IS_FAILOVER,
            },
            /// The lookup key for a {@link android.net.Network Network} object included with the intent after
            /// successfully finding a network for the applications request.  Retrieve it with
            /// {@link android.content.Intent#getParcelableExtra(String)}.
            /// 
            /// Note that if you intend to invoke {@link android.net.Network#openConnection(java.net.URL) Network#openConnection(java.net.URL)}
            /// then you must get a ConnectivityManager instance before doing so.
            #[allow(non_snake_case)]
            static EXTRA_NETWORK {
                sig = JString,
                get = EXTRA_NETWORK,
            },
            /// The lookup key for a {@link android.net.NetworkInfo NetworkInfo} object. Retrieve with
            /// {@link android.content.Intent#getParcelableExtra(String)}.
            ///
            /// can't accurately represent modern network characteristics.
            /// Please obtain information about networks from the {@link android.net.NetworkCapabilities NetworkCapabilities}
            /// or {@link android.net.LinkProperties LinkProperties} objects instead.
            #[deprecated]
            #[allow(non_snake_case)]
            static EXTRA_NETWORK_INFO {
                sig = JString,
                get = EXTRA_NETWORK_INFO,
            },
            /// The lookup key for a {@link android.net.NetworkRequest NetworkRequest} object included with the intent after
            /// successfully finding a network for the applications request.  Retrieve it with
            /// {@link android.content.Intent#getParcelableExtra(String)}.
            #[allow(non_snake_case)]
            static EXTRA_NETWORK_REQUEST {
                sig = JString,
                get = EXTRA_NETWORK_REQUEST,
            },
            /// Network type which triggered a {@link #CONNECTIVITY_ACTION} broadcast.
            ///
            /// of modern networks. Please use {@link android.net.NetworkCapabilities NetworkCapabilities} instead,
            /// in particular the transports.
            #[deprecated]
            #[allow(non_snake_case)]
            static EXTRA_NETWORK_TYPE {
                sig = JString,
                get = EXTRA_NETWORK_TYPE,
            },
            /// The lookup key for a boolean that indicates whether there is a
            /// complete lack of connectivity, i.e., no network is available.
            /// Retrieve it with {@link android.content.Intent#getBooleanExtra(String,boolean)}.
            #[allow(non_snake_case)]
            static EXTRA_NO_CONNECTIVITY {
                sig = JString,
                get = EXTRA_NO_CONNECTIVITY,
            },
            /// The lookup key for a {@link android.net.NetworkInfo NetworkInfo} object. This is supplied when
            /// there is another network that it may be possible to connect to. Retrieve with
            /// {@link android.content.Intent#getParcelableExtra(String)}.
            ///
            #[deprecated]
            #[allow(non_snake_case)]
            static EXTRA_OTHER_NETWORK_INFO {
                sig = JString,
                get = EXTRA_OTHER_NETWORK_INFO,
            },
            /// The lookup key for a string that indicates why an attempt to connect
            /// to a network failed. The string has no particular structure. It is
            /// intended to be used in notifications presented to users. Retrieve
            /// it with {@link android.content.Intent#getStringExtra(String)}.
            #[allow(non_snake_case)]
            static EXTRA_REASON {
                sig = JString,
                get = EXTRA_REASON,
            },
            /// It is acceptable to briefly use multipath data to provide seamless connectivity for
            /// time-sensitive user-facing operations when the system default network is temporarily
            /// unresponsive. The amount of data should be limited (less than one megabyte for every call to
            /// this method), and the operation should be infrequent to ensure that data usage is limited.
            ///
            /// An example of such an operation might be a time-sensitive foreground activity, such as a
            /// voice command, that the user is performing while walking out of range of a Wi-Fi network.
            #[allow(non_snake_case)]
            static MULTIPATH_PREFERENCE_HANDOVER {
                sig = jint,
                get = MULTIPATH_PREFERENCE_HANDOVER,
            },
            /// It is acceptable to use metered data to improve network latency and performance.
            #[allow(non_snake_case)]
            static MULTIPATH_PREFERENCE_PERFORMANCE {
                sig = jint,
                get = MULTIPATH_PREFERENCE_PERFORMANCE,
            },
            /// It is acceptable to use small amounts of multipath data on an ongoing basis to provide
            /// a backup channel for traffic that is primarily going over another network.
            ///
            /// An example might be maintaining backup connections to peers or servers for the purpose of
            /// fast fallback if the default network is temporarily unresponsive or disconnects. The traffic
            /// on backup paths should be negligible compared to the traffic on the main path.
            #[allow(non_snake_case)]
            static MULTIPATH_PREFERENCE_RELIABILITY {
                sig = jint,
                get = MULTIPATH_PREFERENCE_RELIABILITY,
            },
            /// Device is not restricting metered network activity while application is running on
            /// background.
            #[allow(non_snake_case)]
            static RESTRICT_BACKGROUND_STATUS_DISABLED {
                sig = jint,
                get = RESTRICT_BACKGROUND_STATUS_DISABLED,
            },
            /// Device is restricting metered network activity while application is running on background.
            /// 
            /// In this state, application should not try to use the network while running on background,
            /// because it would be denied.
            #[allow(non_snake_case)]
            static RESTRICT_BACKGROUND_STATUS_ENABLED {
                sig = jint,
                get = RESTRICT_BACKGROUND_STATUS_ENABLED,
            },
            /// Device is restricting metered network activity while application is running on background,
            /// but application is allowed to bypass it.
            /// 
            /// In this state, application should take action to mitigate metered network access.
            /// For example, a music streaming application should switch to a low-bandwidth bitrate.
            #[allow(non_snake_case)]
            static RESTRICT_BACKGROUND_STATUS_WHITELISTED {
                sig = jint,
                get = RESTRICT_BACKGROUND_STATUS_WHITELISTED,
            },
            /// A Bluetooth data connection.
            ///
            /// {@link #requestNetwork(android.net.NetworkRequest,android.net.ConnectivityManager.NetworkCallback)} to request an
            /// appropriate network. {@see NetworkCapabilities} for supported transports.
            #[deprecated]
            #[allow(non_snake_case)]
            static TYPE_BLUETOOTH {
                sig = jint,
                get = TYPE_BLUETOOTH,
            },
            /// Fake data connection.  This should not be used on shipping devices.
            #[deprecated]
            #[allow(non_snake_case)]
            static TYPE_DUMMY {
                sig = jint,
                get = TYPE_DUMMY,
            },
            /// An Ethernet data connection.
            ///
            /// {@link #requestNetwork(android.net.NetworkRequest,android.net.ConnectivityManager.NetworkCallback)} to request an
            /// appropriate network. {@see NetworkCapabilities} for supported transports.
            #[deprecated]
            #[allow(non_snake_case)]
            static TYPE_ETHERNET {
                sig = jint,
                get = TYPE_ETHERNET,
            },
            /// A Mobile data connection. Devices may support more than one.
            ///
            /// {@link #requestNetwork(android.net.NetworkRequest,android.net.ConnectivityManager.NetworkCallback)} to request an
            /// appropriate network. {@see NetworkCapabilities} for supported transports.
            #[deprecated]
            #[allow(non_snake_case)]
            static TYPE_MOBILE {
                sig = jint,
                get = TYPE_MOBILE,
            },
            /// A DUN-specific Mobile data connection.  This network type may use the
            /// same network interface as {@link #TYPE_MOBILE} or it may use a different
            /// one.  This is sometimes by the system when setting up an upstream connection
            /// for tethering so that the carrier is aware of DUN traffic.
            ///
            /// {@link #requestNetwork(android.net.NetworkRequest,android.net.ConnectivityManager.NetworkCallback)} to request a network that
            /// provides the {@link android.net.NetworkCapabilities#NET_CAPABILITY_DUN NetworkCapabilities#NET_CAPABILITY_DUN} capability.
            #[deprecated]
            #[allow(non_snake_case)]
            static TYPE_MOBILE_DUN {
                sig = jint,
                get = TYPE_MOBILE_DUN,
            },
            /// A High Priority Mobile data connection.  This network type uses the
            /// same network interface as {@link #TYPE_MOBILE} but the routing setup
            /// is different.
            ///
            /// {@link #requestNetwork(android.net.NetworkRequest,android.net.ConnectivityManager.NetworkCallback)} to request an
            /// appropriate network. {@see NetworkCapabilities} for supported transports.
            #[deprecated]
            #[allow(non_snake_case)]
            static TYPE_MOBILE_HIPRI {
                sig = jint,
                get = TYPE_MOBILE_HIPRI,
            },
            /// An MMS-specific Mobile data connection.  This network type may use the
            /// same network interface as {@link #TYPE_MOBILE} or it may use a different
            /// one.  This is used by applications needing to talk to the carrier's
            /// Multimedia Messaging Service servers.
            ///
            /// {@link #requestNetwork(android.net.NetworkRequest,android.net.ConnectivityManager.NetworkCallback)} to request a network that
            /// provides the {@link android.net.NetworkCapabilities#NET_CAPABILITY_MMS NetworkCapabilities#NET_CAPABILITY_MMS} capability.
            #[deprecated]
            #[allow(non_snake_case)]
            static TYPE_MOBILE_MMS {
                sig = jint,
                get = TYPE_MOBILE_MMS,
            },
            /// A SUPL-specific Mobile data connection.  This network type may use the
            /// same network interface as {@link #TYPE_MOBILE} or it may use a different
            /// one.  This is used by applications needing to talk to the carrier's
            /// Secure User Plane Location servers for help locating the device.
            ///
            /// {@link #requestNetwork(android.net.NetworkRequest,android.net.ConnectivityManager.NetworkCallback)} to request a network that
            /// provides the {@link android.net.NetworkCapabilities#NET_CAPABILITY_SUPL NetworkCapabilities#NET_CAPABILITY_SUPL} capability.
            #[deprecated]
            #[allow(non_snake_case)]
            static TYPE_MOBILE_SUPL {
                sig = jint,
                get = TYPE_MOBILE_SUPL,
            },
            /// A virtual network using one or more native bearers.
            /// It may or may not be providing security services.
            #[deprecated]
            #[allow(non_snake_case)]
            static TYPE_VPN {
                sig = jint,
                get = TYPE_VPN,
            },
            /// A WIFI data connection. Devices may support more than one.
            ///
            /// {@link #requestNetwork(android.net.NetworkRequest,android.net.ConnectivityManager.NetworkCallback)} to request an
            /// appropriate network. {@see NetworkCapabilities} for supported transports.
            #[deprecated]
            #[allow(non_snake_case)]
            static TYPE_WIFI {
                sig = jint,
                get = TYPE_WIFI,
            },
            /// A WiMAX data connection.
            ///
            /// {@link #requestNetwork(android.net.NetworkRequest,android.net.ConnectivityManager.NetworkCallback)} to request an
            /// appropriate network. {@see NetworkCapabilities} for supported transports.
            #[deprecated]
            #[allow(non_snake_case)]
            static TYPE_WIMAX {
                sig = jint,
                get = TYPE_WIMAX,
            },
        },
        methods {
            /// Start listening to reports when the system's default data network is active, meaning it is
            /// a good time to perform network traffic.  Use {@link #isDefaultNetworkActive()}
            /// to determine the current state of the system's default network after registering the
            /// listener.
            /// 
            /// If the process default network has been set with
            /// {@link android.net.ConnectivityManager#bindProcessToNetwork ConnectivityManager#bindProcessToNetwork} this function will not
            /// reflect the process's default, but the system default.
            ///
            ///
            /// # Parameters
            /// * `l` - The listener to be told when the network is active.
            fn add_default_network_active_listener(l: crate::android::net::ConnectivityManagerOnNetworkActiveListener),
            /// Binds the current process to {@code network}.  All Sockets created in the future
            /// (and not explicitly bound via a bound SocketFactory from
            /// {@link android.net.Network#getSocketFactory() Network.getSocketFactory()}) will be bound to
            /// {@code network}.  All host name resolutions will be limited to {@code network} as well.
            /// Note that if {@code network} ever disconnects, all Sockets created in this way will cease to
            /// work and all host name resolutions will fail.  This is by design so an application doesn't
            /// accidentally use Sockets it thinks are still bound to a particular {@link android.net.Network Network}.
            /// To clear binding pass {@code null} for {@code network}.  Using individually bound
            /// Sockets created by Network.getSocketFactory().createSocket() and
            /// performing network-specific host name resolutions via
            /// {@link android.net.Network#getAllByName Network.getAllByName} is preferred to calling
            /// {@code bindProcessToNetwork}.
            ///
            ///
            /// # Parameters
            /// * `network` - The {@link android.net.Network Network} to bind the current process to, or {@code null} to clear
            /// the current binding.
            ///
            /// # Returns
            /// {@code true} on success, {@code false} if the {@link android.net.Network Network} is no longer valid.
            fn bind_process_to_network(network: crate::android::net::Network) -> jboolean,
            /// Request that keepalives be started on a IPsec NAT-T socket.
            ///
            ///
            /// # Parameters
            /// * `network` - The {@link android.net.Network Network} the socket is on.
            /// * `socket` - The socket that needs to be kept alive.
            /// * `source` - The source address of the {@link android.net.IpSecManager.UdpEncapsulationSocket UdpEncapsulationSocket}.
            /// * `destination` - The destination address of the {@link android.net.IpSecManager.UdpEncapsulationSocket UdpEncapsulationSocket}.
            /// * `executor` - The executor on which callback will be invoked. The provided {@link java.util.concurrent.Executor Executor}
            /// must run callback sequentially, otherwise the order of callbacks cannot be
            /// guaranteed.
            ///
            /// # Parameters
            /// * `callback` - A {@link android.net.SocketKeepalive.Callback SocketKeepalive.Callback}. Used for notifications about keepalive
            /// changes. Must be extended by applications that use this API.
            ///
            ///
            /// # Returns
            /// A {@link android.net.SocketKeepalive SocketKeepalive} object that can be used to control the keepalive on the
            /// given socket.
            fn create_socket_keepalive(network: crate::android::net::Network, socket: "android.net.IpSecManager$UdpEncapsulationSocket", source: crate::java::net::InetAddress, destination: crate::java::net::InetAddress, executor: "java.util.concurrent.Executor", callback: "android.net.SocketKeepalive$Callback") -> "android.net.SocketKeepalive",
            /// Returns a {@link android.net.Network Network} object corresponding to the currently active
            /// default data network.  In the event that the current active default data
            /// network disconnects, the returned {@code Network} object will no longer
            /// be usable.  This will return {@code null} when there is no default
            /// network.
            ///
            ///
            /// # Returns
            /// a {@link android.net.Network Network} object for the current default network or
            /// {@code null} if no default network is currently active
            fn get_active_network() -> crate::android::net::Network,
            /// Returns details about the currently active default data network. When
            /// connected, this network is the default route for outgoing connections.
            /// You should always check {@link android.net.NetworkInfo#isConnected() NetworkInfo#isConnected()} before initiating
            /// network traffic. This may return {@code null} when there is no default
            /// network.
            /// Note that if the default network is a VPN, this method will return the
            /// NetworkInfo for one of its underlying networks instead, or null if the
            /// VPN agent did not specify any. Apps interested in learning about VPNs
            /// should use {@link #getNetworkInfo(android.net.Network)} instead.
            ///
            ///
            /// # Returns
            /// a {@link android.net.NetworkInfo NetworkInfo} object for the current default network
            /// or {@code null} if no default network is currently active
            #[deprecated]
            fn get_active_network_info() -> "android.net.NetworkInfo",
            /// Returns connection status information about all network
            /// types supported by the device.
            ///
            ///
            /// # Returns
            /// an array of {@link android.net.NetworkInfo NetworkInfo} objects.  Check each
            /// {@link android.net.NetworkInfo#getType NetworkInfo#getType} for which type each applies.
            ///
            /// of the same type. Use {@link #getAllNetworks} and
            /// {@link #getNetworkInfo(android.net.Network)} instead.
            #[deprecated]
            fn get_all_network_info() -> "android.net.NetworkInfo"[],
            /// Returns an array of all {@link android.net.Network Network} currently tracked by the
            /// framework.
            ///
            /// apps to call it repeatedly. This is inefficient and prone to race conditions.
            /// Apps should use methods such as
            /// {@link #registerNetworkCallback(android.net.NetworkRequest,android.net.ConnectivityManager.NetworkCallback)} instead.
            /// Apps that desire to obtain information about networks that do not apply to them
            /// can use {@link android.net.NetworkRequest.Builder#setIncludeOtherUidNetworks NetworkRequest.Builder#setIncludeOtherUidNetworks}.
            ///
            ///
            /// # Returns
            /// an array of {@link android.net.Network Network} objects.
            #[deprecated]
            fn get_all_networks() -> crate::android::net::Network[],
            /// Returns the value of the setting for background data usage. If false,
            /// applications should not use the network if the application is not in the
            /// foreground. Developers should respect this setting, and check the value
            /// of this before performing any background data operations.
            /// 
            /// All applications that have background services that use the network
            /// should listen to {@link #ACTION_BACKGROUND_DATA_SETTING_CHANGED}.
            /// 
            /// background data depends on several combined factors, and this method will
            /// always return {@code true}. Instead, when background data is unavailable,
            /// {@link #getActiveNetworkInfo()} will now appear disconnected.
            ///
            ///
            /// # Returns
            /// Whether background data usage is allowed.
            #[deprecated]
            fn get_background_data_setting() -> jboolean,
            /// Returns the {@link android.net.Network Network} currently bound to this process via
            /// {@link #bindProcessToNetwork}, or {@code null} if no {@link android.net.Network Network} is explicitly bound.
            ///
            ///
            /// # Returns
            /// {@code Network} to which this process is bound, or {@code null}.
            fn get_bound_network_for_process() -> crate::android::net::Network,
            /// Returns the {@code uid} of the owner of a network connection.
            ///
            ///
            /// # Parameters
            /// * `protocol` - The protocol of the connection. Only {@code IPPROTO_TCP} and {@code
            /// IPPROTO_UDP} currently supported.
            ///
            /// # Parameters
            /// * `local` - The local {@link java.net.InetSocketAddress InetSocketAddress} of a connection.
            /// * `remote` - The remote {@link java.net.InetSocketAddress InetSocketAddress} of a connection.
            ///
            /// # Returns
            /// {@code uid} if the connection is found and the app has permission to observe it
            /// (e.g., if it is associated with the calling VPN app's VpnService tunnel) or {@link
            /// android.os.Process#INVALID_UID} if the connection is not found.
            /// user.
            fn get_connection_owner_uid(protocol: jint, local: "java.net.InetSocketAddress", remote: "java.net.InetSocketAddress") -> jint,
            /// Get the current default HTTP proxy settings.  If a global proxy is set it will be returned,
            /// otherwise if this process is bound to a {@link android.net.Network Network} using
            /// {@link #bindProcessToNetwork} then that {@code Network}'s proxy is returned, otherwise
            /// the default network's proxy is returned.
            ///
            ///
            /// # Returns
            /// the {@link android.net.ProxyInfo ProxyInfo} for the current HTTP proxy, or {@code null} if no
            /// HTTP proxy is active.
            fn get_default_proxy() -> "android.net.ProxyInfo",
            /// Get the {@link android.net.LinkProperties LinkProperties} for the given {@link android.net.Network Network}.  This
            /// will return {@code null} if the network is unknown.
            ///
            ///
            /// # Parameters
            /// * `network` - The {@link android.net.Network Network} object identifying the network in question.
            ///
            /// # Returns
            /// The {@link android.net.LinkProperties LinkProperties} for the network, or {@code null}.
            fn get_link_properties(network: crate::android::net::Network) -> crate::android::net::LinkProperties,
            /// Provides a hint to the calling application on whether it is desirable to use the
            /// multinetwork APIs (e.g., {@link android.net.Network#openConnection Network#openConnection}, {@link android.net.Network#bindSocket Network#bindSocket}, etc.)
            /// for multipath data transfer on this network when it is not the system default network.
            /// Applications desiring to use multipath network protocols should call this method before
            /// each such operation.
            ///
            ///
            /// # Parameters
            /// * `network` - The network on which the application desires to use multipath data.
            /// If {@code null}, this method will return the a preference that will generally
            /// apply to metered networks.
            ///
            /// # Returns
            /// a bitwise OR of zero or more of the  {@code MULTIPATH_PREFERENCE_*} constants.
            fn get_multipath_preference(network: crate::android::net::Network) -> jint,
            /// Get the {@link android.net.NetworkCapabilities NetworkCapabilities} for the given {@link android.net.Network Network}.  This
            /// will return {@code null} if the network is unknown or if the |network| argument is null.
            ///
            /// This will remove any location sensitive data in {@link android.net.TransportInfo TransportInfo} embedded in
            /// {@link android.net.NetworkCapabilities#getTransportInfo() NetworkCapabilities#getTransportInfo()}. Some transport info instances like
            /// {@link android.net.wifi.WifiInfo} contain location sensitive information. Retrieving
            /// this location sensitive information (subject to app's location permissions) will be
            /// noted by system. To include any location sensitive data in {@link android.net.TransportInfo TransportInfo},
            /// use a {@link android.net.ConnectivityManager.NetworkCallback NetworkCallback} with
            /// {@link android.net.ConnectivityManager.NetworkCallback#FLAG_INCLUDE_LOCATION_INFO NetworkCallback#FLAG_INCLUDE_LOCATION_INFO} flag.
            ///
            ///
            /// # Parameters
            /// * `network` - The {@link android.net.Network Network} object identifying the network in question.
            ///
            /// # Returns
            /// The {@link android.net.NetworkCapabilities NetworkCapabilities} for the network, or {@code null}.
            fn get_network_capabilities(network: crate::android::net::Network) -> crate::android::net::NetworkCapabilities,
            /// Returns connection status information about a particular
            /// Network.
            ///
            ///
            /// # Parameters
            /// * `network` - {@link android.net.Network Network} specifying which network
            /// in which you're interested.
            ///
            /// # Returns
            /// a {@link android.net.NetworkInfo NetworkInfo} object for the requested
            /// network or {@code null} if the {@code Network}
            /// is not valid.
            #[deprecated]
            fn get_network_info_network {
                name = "getNetworkInfo",
                sig = (network: crate::android::net::Network) -> "android.net.NetworkInfo",
            },
            /// Returns connection status information about a particular
            /// network type.
            ///
            ///
            /// # Parameters
            /// * `network_type` - integer specifying which networkType in
            /// which you're interested.
            ///
            /// # Returns
            /// a {@link android.net.NetworkInfo NetworkInfo} object for the requested
            /// network type or {@code null} if the type is not
            /// supported by the device. If {@code networkType} is
            /// TYPE_VPN and a VPN is active for the calling app,
            /// then this method will try to return one of the
            /// underlying networks for the VPN or null if the
            /// VPN agent didn't specify any.
            ///
            /// of the same type. Use {@link #getAllNetworks} and
            /// {@link #getNetworkInfo(android.net.Network)} instead.
            #[deprecated]
            fn get_network_info_int {
                name = "getNetworkInfo",
                sig = (network_type: jint) -> "android.net.NetworkInfo",
            },
            /// Retrieves the current preferred network type.
            ///
            ///
            /// # Returns
            /// an integer representing the preferred network type
            /// with many more than two networks - we'd need an array to express
            /// preference.  Instead we use dynamic network properties of
            /// the networks to describe their precedence.
            #[deprecated]
            fn get_network_preference() -> jint,
            /// The network watchlist is a list of domains and IP addresses that are associated with
            /// potentially harmful apps. This method returns the SHA-256 of the watchlist config file
            /// currently used by the system for validation purposes.
            ///
            ///
            /// # Returns
            /// Hash of network watchlist config file. Null if config does not exist.
            fn get_network_watchlist_config_hash() -> jbyte[],
            /// Returns the {@link android.net.Network Network} currently bound to this process via
            /// {@link #bindProcessToNetwork}, or {@code null} if no {@link android.net.Network Network} is explicitly bound.
            ///
            ///
            /// # Returns
            /// {@code Network} to which this process is bound, or {@code null}.
            /// {@link java.lang.IllegalStateException IllegalStateException}.  Use {@link #getBoundNetworkForProcess} instead.
            /// {@code getBoundNetworkForProcess} is a direct replacement.
            #[deprecated]
            static fn get_process_default_network() -> crate::android::net::Network,
            /// Determines if the calling application is subject to metered network restrictions while
            /// running on background.
            ///
            ///
            /// # Returns
            /// {@link #RESTRICT_BACKGROUND_STATUS_DISABLED},
            /// {@link #RESTRICT_BACKGROUND_STATUS_ENABLED},
            /// or {@link #RESTRICT_BACKGROUND_STATUS_WHITELISTED}
            fn get_restrict_background_status() -> jint,
            /// Returns if the currently active data network is metered. A network is
            /// classified as metered when the user is sensitive to heavy data usage on
            /// that connection due to monetary costs, data limitations or
            /// battery/performance issues. You should check this before doing large
            /// data transfers, and warn the user or delay the operation until another
            /// network is available.
            ///
            ///
            /// # Returns
            /// {@code true} if large transfers should be avoided, otherwise
            /// {@code false}.
            fn is_active_network_metered() -> jboolean,
            /// Return whether the data network is currently active.  An active network means that
            /// it is currently in a high power state for performing data transmission.  On some
            /// types of networks, it may be expensive to move and stay in such a state, so it is
            /// more power efficient to batch network traffic together when the radio is already in
            /// this state.  This method tells you whether right now is currently a good time to
            /// initiate network traffic, as the network is already active.
            fn is_default_network_active() -> jboolean,
            /// Tests if a given integer represents a valid network type.
            ///
            /// # Parameters
            /// * `network_type` - the type to be tested
            ///
            /// # Returns
            /// a boolean.  {@code true} if the type is valid, else {@code false}
            /// validate a network type.
            #[deprecated]
            static fn is_network_type_valid(network_type: jint) -> jboolean,
            /// Registers to receive notifications about the best matching network which satisfy the given
            /// {@link android.net.NetworkRequest NetworkRequest}.  The callbacks will continue to be called until
            /// either the application exits or {@link #unregisterNetworkCallback(android.net.ConnectivityManager.NetworkCallback)} is
            /// called.
            ///
            /// To avoid performance issues due to apps leaking callbacks, the system will limit the
            /// number of outstanding requests to 100 per app (identified by their UID), shared with
            /// {@link #registerNetworkCallback} and its variants and {@link #requestNetwork} as well as
            /// {@link android.net.ConnectivityDiagnosticsManager#registerConnectivityDiagnosticsCallback ConnectivityDiagnosticsManager#registerConnectivityDiagnosticsCallback}.
            /// Requesting a network with this method will count toward this limit. If this limit is
            /// exceeded, an exception will be thrown. To avoid hitting this issue and to conserve resources,
            /// make sure to unregister the callbacks with
            /// {@link #unregisterNetworkCallback(android.net.ConnectivityManager.NetworkCallback)}.
            ///
            ///
            ///
            /// # Parameters
            /// * `request` - {@link android.net.NetworkRequest NetworkRequest} describing this request.
            /// * `network_callback` - The {@link android.net.ConnectivityManager.NetworkCallback NetworkCallback} that the system will call as suitable
            /// networks change state.
            ///
            /// # Parameters
            /// * `handler` - {@link android.os.Handler Handler} to specify the thread upon which the callback will be invoked.
            fn register_best_matching_network_callback(request: "android.net.NetworkRequest", network_callback: crate::android::net::ConnectivityManagerNetworkCallback, handler: "android.os.Handler"),
            /// Registers to receive notifications about changes in the application's default network. This
            /// may be a physical network or a virtual network, such as a VPN that applies to the
            /// application. The callbacks will continue to be called until either the application exits or
            /// {@link #unregisterNetworkCallback(android.net.ConnectivityManager.NetworkCallback)} is called.
            ///
            /// To avoid performance issues due to apps leaking callbacks, the system will limit the
            /// number of outstanding requests to 100 per app (identified by their UID), shared with
            /// all variants of this method, of {@link #requestNetwork} as well as
            /// {@link android.net.ConnectivityDiagnosticsManager#registerConnectivityDiagnosticsCallback ConnectivityDiagnosticsManager#registerConnectivityDiagnosticsCallback}.
            /// Requesting a network with this method will count toward this limit. If this limit is
            /// exceeded, an exception will be thrown. To avoid hitting this issue and to conserve resources,
            /// make sure to unregister the callbacks with
            /// {@link #unregisterNetworkCallback(android.net.ConnectivityManager.NetworkCallback)}.
            ///
            ///
            /// # Parameters
            /// * `network_callback` - The {@link android.net.ConnectivityManager.NetworkCallback NetworkCallback} that the system will call as the
            /// application's default network changes.
            /// The callback is invoked on the default internal Handler.
            fn register_default_network_callback {
                name = "registerDefaultNetworkCallback",
                sig = (network_callback: crate::android::net::ConnectivityManagerNetworkCallback),
            },
            /// Registers to receive notifications about changes in the application's default network. This
            /// may be a physical network or a virtual network, such as a VPN that applies to the
            /// application. The callbacks will continue to be called until either the application exits or
            /// {@link #unregisterNetworkCallback(android.net.ConnectivityManager.NetworkCallback)} is called.
            ///
            /// To avoid performance issues due to apps leaking callbacks, the system will limit the
            /// number of outstanding requests to 100 per app (identified by their UID), shared with
            /// all variants of this method, of {@link #requestNetwork} as well as
            /// {@link android.net.ConnectivityDiagnosticsManager#registerConnectivityDiagnosticsCallback ConnectivityDiagnosticsManager#registerConnectivityDiagnosticsCallback}.
            /// Requesting a network with this method will count toward this limit. If this limit is
            /// exceeded, an exception will be thrown. To avoid hitting this issue and to conserve resources,
            /// make sure to unregister the callbacks with
            /// {@link #unregisterNetworkCallback(android.net.ConnectivityManager.NetworkCallback)}.
            ///
            ///
            /// # Parameters
            /// * `network_callback` - The {@link android.net.ConnectivityManager.NetworkCallback NetworkCallback} that the system will call as the
            /// application's default network changes.
            ///
            /// # Parameters
            /// * `handler` - {@link android.os.Handler Handler} to specify the thread upon which the callback will be invoked.
            #[allow(non_snake_case)]
            fn register_default_network_callback2 {
                name = "registerDefaultNetworkCallback",
                sig = (network_callback: crate::android::net::ConnectivityManagerNetworkCallback, handler: "android.os.Handler"),
            },
            /// Registers a PendingIntent to be sent when a network is available which satisfies the given
            /// {@link android.net.NetworkRequest NetworkRequest}.
            ///
            /// This function behaves identically to the version that takes a NetworkCallback, but instead
            /// of {@link android.net.ConnectivityManager.NetworkCallback NetworkCallback} a {@link android.app.PendingIntent PendingIntent} is used.  This means
            /// the request may outlive the calling application and get called back when a suitable
            /// network is found.
            /// 
            /// The operation is an Intent broadcast that goes to a broadcast receiver that
            /// you registered with {@link android.content.Context#registerReceiver Context#registerReceiver} or through the
            /// <receiver> tag in an AndroidManifest.xml file
            /// 
            /// The operation Intent is delivered with two extras, a {@link android.net.Network Network} typed
            /// extra called {@link #EXTRA_NETWORK} and a {@link android.net.NetworkRequest NetworkRequest}
            /// typed extra called {@link #EXTRA_NETWORK_REQUEST} containing
            /// the original requests parameters.
            /// 
            /// If there is already a request for this Intent registered (with the equality of
            /// two Intents defined by {@link android.content.Intent#filterEquals Intent#filterEquals}), then it will be removed and
            /// replaced by this one, effectively releasing the previous {@link android.net.NetworkRequest NetworkRequest}.
            /// 
            /// The request may be released normally by calling
            /// {@link #unregisterNetworkCallback(android.app.PendingIntent)}.
            ///
            /// To avoid performance issues due to apps leaking callbacks, the system will limit the
            /// number of outstanding requests to 100 per app (identified by their UID), shared with
            /// all variants of this method, of {@link #requestNetwork} as well as
            /// {@link android.net.ConnectivityDiagnosticsManager#registerConnectivityDiagnosticsCallback ConnectivityDiagnosticsManager#registerConnectivityDiagnosticsCallback}.
            /// Requesting a network with this method will count toward this limit. If this limit is
            /// exceeded, an exception will be thrown. To avoid hitting this issue and to conserve resources,
            /// make sure to unregister the callbacks with {@link #unregisterNetworkCallback(android.app.PendingIntent)}
            /// or {@link #releaseNetworkRequest(android.app.PendingIntent)}.
            ///
            ///
            /// # Parameters
            /// * `request` - {@link android.net.NetworkRequest NetworkRequest} describing this request.
            /// * `operation` - Action to perform when the network is available (corresponds
            /// to the {@link android.net.ConnectivityManager.NetworkCallback#onAvailable NetworkCallback#onAvailable} call.  Typically
            /// comes from {@link android.app.PendingIntent#getBroadcast PendingIntent#getBroadcast}. Cannot be null.
            #[allow(non_snake_case)]
            fn register_network_callback2_pending_intent {
                name = "registerNetworkCallback",
                sig = (request: "android.net.NetworkRequest", operation: "android.app.PendingIntent"),
            },
            /// Registers to receive notifications about all networks which satisfy the given
            /// {@link android.net.NetworkRequest NetworkRequest}.  The callbacks will continue to be called until
            /// either the application exits or {@link #unregisterNetworkCallback(android.net.ConnectivityManager.NetworkCallback)} is
            /// called.
            ///
            /// To avoid performance issues due to apps leaking callbacks, the system will limit the
            /// number of outstanding requests to 100 per app (identified by their UID), shared with
            /// all variants of this method, of {@link #requestNetwork} as well as
            /// {@link android.net.ConnectivityDiagnosticsManager#registerConnectivityDiagnosticsCallback ConnectivityDiagnosticsManager#registerConnectivityDiagnosticsCallback}.
            /// Requesting a network with this method will count toward this limit. If this limit is
            /// exceeded, an exception will be thrown. To avoid hitting this issue and to conserve resources,
            /// make sure to unregister the callbacks with
            /// {@link #unregisterNetworkCallback(android.net.ConnectivityManager.NetworkCallback)}.
            ///
            ///
            /// # Parameters
            /// * `request` - {@link android.net.NetworkRequest NetworkRequest} describing this request.
            /// * `network_callback` - The {@link android.net.ConnectivityManager.NetworkCallback NetworkCallback} that the system will call as suitable
            /// networks change state.
            /// The callback is invoked on the default internal Handler.
            #[allow(non_snake_case)]
            fn register_network_callback2_connectivity_manager_network_callback {
                name = "registerNetworkCallback",
                sig = (request: "android.net.NetworkRequest", network_callback: crate::android::net::ConnectivityManagerNetworkCallback),
            },
            /// Registers to receive notifications about all networks which satisfy the given
            /// {@link android.net.NetworkRequest NetworkRequest}.  The callbacks will continue to be called until
            /// either the application exits or {@link #unregisterNetworkCallback(android.net.ConnectivityManager.NetworkCallback)} is
            /// called.
            ///
            /// To avoid performance issues due to apps leaking callbacks, the system will limit the
            /// number of outstanding requests to 100 per app (identified by their UID), shared with
            /// all variants of this method, of {@link #requestNetwork} as well as
            /// {@link android.net.ConnectivityDiagnosticsManager#registerConnectivityDiagnosticsCallback ConnectivityDiagnosticsManager#registerConnectivityDiagnosticsCallback}.
            /// Requesting a network with this method will count toward this limit. If this limit is
            /// exceeded, an exception will be thrown. To avoid hitting this issue and to conserve resources,
            /// make sure to unregister the callbacks with
            /// {@link #unregisterNetworkCallback(android.net.ConnectivityManager.NetworkCallback)}.
            ///
            ///
            ///
            /// # Parameters
            /// * `request` - {@link android.net.NetworkRequest NetworkRequest} describing this request.
            /// * `network_callback` - The {@link android.net.ConnectivityManager.NetworkCallback NetworkCallback} that the system will call as suitable
            /// networks change state.
            ///
            /// # Parameters
            /// * `handler` - {@link android.os.Handler Handler} to specify the thread upon which the callback will be invoked.
            fn register_network_callback {
                name = "registerNetworkCallback",
                sig = (request: "android.net.NetworkRequest", network_callback: crate::android::net::ConnectivityManagerNetworkCallback, handler: "android.os.Handler"),
            },
            /// Removes a request made via {@link #requestNetwork(android.net.NetworkRequest,android.app.PendingIntent)}
            /// 
            /// This method has the same behavior as
            /// {@link #unregisterNetworkCallback(android.app.PendingIntent)} with respect to
            /// releasing network resources and disconnecting.
            ///
            ///
            /// # Parameters
            /// * `operation` - A PendingIntent equal (as defined by {@link android.content.Intent#filterEquals Intent#filterEquals}) to the
            /// PendingIntent passed to
            /// {@link #requestNetwork(android.net.NetworkRequest,android.app.PendingIntent)} with the
            /// corresponding NetworkRequest you'd like to remove. Cannot be null.
            fn release_network_request(operation: "android.app.PendingIntent"),
            /// Remove network active listener previously registered with
            /// {@link #addDefaultNetworkActiveListener}.
            ///
            ///
            /// # Parameters
            /// * `l` - Previously registered listener.
            fn remove_default_network_active_listener(l: crate::android::net::ConnectivityManagerOnNetworkActiveListener),
            /// Report a problem network to the framework.  This provides a hint to the system
            /// that there might be connectivity problems on this network and may cause
            /// the framework to re-evaluate network connectivity and/or switch to another
            /// network.
            ///
            ///
            /// # Parameters
            /// * `network` - The {@link android.net.Network Network} the application was attempting to use
            /// or {@code null} to indicate the current default network.
            /// working and non-working connectivity.
            #[deprecated]
            fn report_bad_network(network: crate::android::net::Network),
            /// Report to the framework whether a network has working connectivity.
            /// This provides a hint to the system that a particular network is providing
            /// working connectivity or not.  In response the framework may re-evaluate
            /// the network's connectivity and might take further action thereafter.
            ///
            ///
            /// # Parameters
            /// * `network` - The {@link android.net.Network Network} the application was attempting to use
            /// or {@code null} to indicate the current default network.
            ///
            /// # Parameters
            /// * `has_connectivity` - {@code true} if the application was able to successfully access the
            /// Internet using {@code network} or {@code false} if not.
            fn report_network_connectivity(network: crate::android::net::Network, has_connectivity: jboolean),
            /// Requests bandwidth update for a given {@link android.net.Network Network} and returns whether the update request
            /// is accepted by ConnectivityService. Once accepted, ConnectivityService will poll underlying
            /// network connection for updated bandwidth information. The caller will be notified via
            /// {@link android.net.ConnectivityManager.NetworkCallback ConnectivityManager.NetworkCallback} if there is an update. Notice that this
            /// method assumes that the caller has previously called
            /// {@link #registerNetworkCallback(android.net.NetworkRequest,android.net.ConnectivityManager.NetworkCallback)} to listen for network
            /// changes.
            ///
            ///
            /// # Parameters
            /// * `network` - {@link android.net.Network Network} specifying which network you're interested.
            ///
            /// # Returns
            /// {@code true} on success, {@code false} if the {@link android.net.Network Network} is no longer valid.
            fn request_bandwidth_update(network: crate::android::net::Network) -> jboolean,
            /// Request a network to satisfy a set of {@link android.net.NetworkCapabilities NetworkCapabilities}.
            ///
            /// This function behaves identically to the version that takes a NetworkCallback, but instead
            /// of {@link android.net.ConnectivityManager.NetworkCallback NetworkCallback} a {@link android.app.PendingIntent PendingIntent} is used.  This means
            /// the request may outlive the calling application and get called back when a suitable
            /// network is found.
            /// 
            /// The operation is an Intent broadcast that goes to a broadcast receiver that
            /// you registered with {@link android.content.Context#registerReceiver Context#registerReceiver} or through the
            /// <receiver> tag in an AndroidManifest.xml file
            /// 
            /// The operation Intent is delivered with two extras, a {@link android.net.Network Network} typed
            /// extra called {@link #EXTRA_NETWORK} and a {@link android.net.NetworkRequest NetworkRequest}
            /// typed extra called {@link #EXTRA_NETWORK_REQUEST} containing
            /// the original requests parameters.  It is important to create a new,
            /// {@link android.net.ConnectivityManager.NetworkCallback NetworkCallback} based request before completing the processing of the
            /// Intent to reserve the network or it will be released shortly after the Intent
            /// is processed.
            /// 
            /// If there is already a request for this Intent registered (with the equality of
            /// two Intents defined by {@link android.content.Intent#filterEquals Intent#filterEquals}), then it will be removed and
            /// replaced by this one, effectively releasing the previous {@link android.net.NetworkRequest NetworkRequest}.
            /// 
            /// The request may be released normally by calling
            /// {@link #releaseNetworkRequest(android.app.PendingIntent)}.
            /// It is presently unsupported to request a network with either
            /// {@link android.net.NetworkCapabilities#NET_CAPABILITY_VALIDATED NetworkCapabilities#NET_CAPABILITY_VALIDATED} or
            /// {@link android.net.NetworkCapabilities#NET_CAPABILITY_CAPTIVE_PORTAL NetworkCapabilities#NET_CAPABILITY_CAPTIVE_PORTAL}
            /// as these {@code NetworkCapabilities} represent states that a particular
            /// network may never attain, and whether a network will attain these states
            /// is unknown prior to bringing up the network so the framework does not
            /// know how to go about satisfying a request with these capabilities.
            ///
            /// To avoid performance issues due to apps leaking callbacks, the system will limit the
            /// number of outstanding requests to 100 per app (identified by their UID), shared with
            /// all variants of this method, of {@link #registerNetworkCallback} as well as
            /// {@link android.net.ConnectivityDiagnosticsManager#registerConnectivityDiagnosticsCallback ConnectivityDiagnosticsManager#registerConnectivityDiagnosticsCallback}.
            /// Requesting a network with this method will count toward this limit. If this limit is
            /// exceeded, an exception will be thrown. To avoid hitting this issue and to conserve resources,
            /// make sure to unregister the callbacks with {@link #unregisterNetworkCallback(android.app.PendingIntent)}
            /// or {@link #releaseNetworkRequest(android.app.PendingIntent)}.
            ///
            /// This method requires the caller to hold either the
            /// {@link android.Manifest.permission#CHANGE_NETWORK_STATE} permission
            /// or the ability to modify system settings as determined by
            /// {@link android.provider.Settings.System#canWrite}.
            ///
            ///
            /// # Parameters
            /// * `request` - {@link android.net.NetworkRequest NetworkRequest} describing this request.
            /// * `operation` - Action to perform when the network is available (corresponds
            /// to the {@link android.net.ConnectivityManager.NetworkCallback#onAvailable NetworkCallback#onAvailable} call.  Typically
            /// comes from {@link android.app.PendingIntent#getBroadcast PendingIntent#getBroadcast}. Cannot be null.
            #[allow(non_snake_case)]
            fn request_network2_pending_intent {
                name = "requestNetwork",
                sig = (request: "android.net.NetworkRequest", operation: "android.app.PendingIntent"),
            },
            /// Request a network to satisfy a set of {@link android.net.NetworkCapabilities NetworkCapabilities}.
            ///
            /// This method will attempt to find the best network that matches the passed
            /// {@link android.net.NetworkRequest NetworkRequest}, and to bring up one that does if none currently satisfies the
            /// criteria. The platform will evaluate which network is the best at its own discretion.
            /// Throughput, latency, cost per byte, policy, user preference and other considerations
            /// may be factored in the decision of what is considered the best network.
            ///
            /// As long as this request is outstanding, the platform will try to maintain the best network
            /// matching this request, while always attempting to match the request to a better network if
            /// possible. If a better match is found, the platform will switch this request to the now-best
            /// network and inform the app of the newly best network by invoking
            /// {@link android.net.ConnectivityManager.NetworkCallback#onAvailable(android.net.Network) NetworkCallback#onAvailable(Network)} on the provided callback. Note that the platform
            /// will not try to maintain any other network than the best one currently matching the request:
            /// a network not matching any network request may be disconnected at any time.
            ///
            /// For example, an application could use this method to obtain a connected cellular network
            /// even if the device currently has a data connection over Ethernet. This may cause the cellular
            /// radio to consume additional power. Or, an application could inform the system that it wants
            /// a network supporting sending MMSes and have the system let it know about the currently best
            /// MMS-supporting network through the provided {@link android.net.ConnectivityManager.NetworkCallback NetworkCallback}.
            ///
            /// The status of the request can be followed by listening to the various callbacks described
            /// in {@link android.net.ConnectivityManager.NetworkCallback NetworkCallback}. The {@link android.net.Network Network} object passed to the callback methods can be
            /// used to direct traffic to the network (although accessing some networks may be subject to
            /// holding specific permissions). Callers will learn about the specific characteristics of the
            /// network through
            /// {@link android.net.ConnectivityManager.NetworkCallback#onCapabilitiesChanged(android.net.Network,android.net.NetworkCapabilities) NetworkCallback#onCapabilitiesChanged(Network, NetworkCapabilities)} and
            /// {@link android.net.ConnectivityManager.NetworkCallback#onLinkPropertiesChanged(android.net.Network,android.net.LinkProperties) NetworkCallback#onLinkPropertiesChanged(Network, LinkProperties)}. The methods of the
            /// provided {@link android.net.ConnectivityManager.NetworkCallback NetworkCallback} will only be invoked due to changes in the best network
            /// matching the request at any given time; therefore when a better network matching the request
            /// becomes available, the {@link android.net.ConnectivityManager.NetworkCallback#onAvailable(android.net.Network) NetworkCallback#onAvailable(Network)} method is called
            /// with the new network after which no further updates are given about the previously-best
            /// network, unless it becomes the best again at some later time. All callbacks are invoked
            /// in order on the same thread, which by default is a thread created by the framework running
            /// in the app.
            /// {@see #requestNetwork(NetworkRequest, NetworkCallback, Handler)} to change where the
            /// callbacks are invoked.
            ///
            /// This{@link android.net.NetworkRequest NetworkRequest} will live until released via
            /// {@link #unregisterNetworkCallback(android.net.ConnectivityManager.NetworkCallback)} or the calling application exits, at
            /// which point the system may let go of the network at any time.
            ///
            /// A version of this method which takes a timeout is
            /// {@link #requestNetwork(android.net.NetworkRequest,android.net.ConnectivityManager.NetworkCallback,int)}, that an app can use to only
            /// wait for a limited amount of time for the network to become unavailable.
            ///
            /// It is presently unsupported to request a network with mutable
            /// {@link android.net.NetworkCapabilities NetworkCapabilities} such as
            /// {@link android.net.NetworkCapabilities#NET_CAPABILITY_VALIDATED NetworkCapabilities#NET_CAPABILITY_VALIDATED} or
            /// {@link android.net.NetworkCapabilities#NET_CAPABILITY_CAPTIVE_PORTAL NetworkCapabilities#NET_CAPABILITY_CAPTIVE_PORTAL}
            /// as these {@code NetworkCapabilities} represent states that a particular
            /// network may never attain, and whether a network will attain these states
            /// is unknown prior to bringing up the network so the framework does not
            /// know how to go about satisfying a request with these capabilities.
            ///
            /// This method requires the caller to hold either the
            /// {@link android.Manifest.permission#CHANGE_NETWORK_STATE} permission
            /// or the ability to modify system settings as determined by
            /// {@link android.provider.Settings.System#canWrite}.
            ///
            /// To avoid performance issues due to apps leaking callbacks, the system will limit the
            /// number of outstanding requests to 100 per app (identified by their UID), shared with
            /// all variants of this method, of {@link #registerNetworkCallback} as well as
            /// {@link android.net.ConnectivityDiagnosticsManager#registerConnectivityDiagnosticsCallback ConnectivityDiagnosticsManager#registerConnectivityDiagnosticsCallback}.
            /// Requesting a network with this method will count toward this limit. If this limit is
            /// exceeded, an exception will be thrown. To avoid hitting this issue and to conserve resources,
            /// make sure to unregister the callbacks with
            /// {@link #unregisterNetworkCallback(android.net.ConnectivityManager.NetworkCallback)}.
            ///
            ///
            /// # Parameters
            /// * `request` - {@link android.net.NetworkRequest NetworkRequest} describing this request.
            /// * `network_callback` - The {@link android.net.ConnectivityManager.NetworkCallback NetworkCallback} to be utilized for this request. Note
            /// the callback must not be shared - it uniquely specifies this request.
            /// The callback is invoked on the default internal Handler.
            #[allow(non_snake_case)]
            fn request_network2_connectivity_manager_network_callback {
                name = "requestNetwork",
                sig = (request: "android.net.NetworkRequest", network_callback: crate::android::net::ConnectivityManagerNetworkCallback),
            },
            /// Request a network to satisfy a set of {@link android.net.NetworkCapabilities NetworkCapabilities}.
            ///
            /// This method behaves identically to {@link #requestNetwork(android.net.NetworkRequest,android.net.ConnectivityManager.NetworkCallback)}
            /// but runs all the callbacks on the passed Handler.
            ///
            /// This method has the same permission requirements as
            /// {@link #requestNetwork(android.net.NetworkRequest,android.net.ConnectivityManager.NetworkCallback)}, is subject to the same limitations,
            /// and throws the same exceptions in the same conditions.
            ///
            ///
            /// # Parameters
            /// * `request` - {@link android.net.NetworkRequest NetworkRequest} describing this request.
            /// * `network_callback` - The {@link android.net.ConnectivityManager.NetworkCallback NetworkCallback} to be utilized for this request. Note
            /// the callback must not be shared - it uniquely specifies this request.
            ///
            /// # Parameters
            /// * `handler` - {@link android.os.Handler Handler} to specify the thread upon which the callback will be invoked.
            #[allow(non_snake_case)]
            fn request_network3_handler {
                name = "requestNetwork",
                sig = (request: "android.net.NetworkRequest", network_callback: crate::android::net::ConnectivityManagerNetworkCallback, handler: "android.os.Handler"),
            },
            /// Request a network to satisfy a set of {@link android.net.NetworkCapabilities NetworkCapabilities}, limited
            /// by a timeout.
            ///
            /// This function behaves identically to the non-timed-out version
            /// {@link #requestNetwork(android.net.NetworkRequest,android.net.ConnectivityManager.NetworkCallback)}, but if a suitable network
            /// is not found within the given time (in milliseconds) the
            /// {@link android.net.ConnectivityManager.NetworkCallback#onUnavailable() NetworkCallback#onUnavailable()} callback is called. The request can still be
            /// released normally by calling {@link #unregisterNetworkCallback(android.net.ConnectivityManager.NetworkCallback)} but does
            /// not have to be released if timed-out (it is automatically released). Unregistering a
            /// request that timed out is not an error.
            ///
            /// Do not use this method to poll for the existence of specific networks (e.g. with a small
            /// timeout) - {@link #registerNetworkCallback(android.net.NetworkRequest,android.net.ConnectivityManager.NetworkCallback)} is provided
            /// for that purpose. Calling this method will attempt to bring up the requested network.
            ///
            /// This method has the same permission requirements as
            /// {@link #requestNetwork(android.net.NetworkRequest,android.net.ConnectivityManager.NetworkCallback)}, is subject to the same limitations,
            /// and throws the same exceptions in the same conditions.
            ///
            ///
            /// # Parameters
            /// * `request` - {@link android.net.NetworkRequest NetworkRequest} describing this request.
            /// * `network_callback` - The {@link android.net.ConnectivityManager.NetworkCallback NetworkCallback} to be utilized for this request. Note
            /// the callback must not be shared - it uniquely specifies this request.
            ///
            /// # Parameters
            /// * `timeout_ms` - The time in milliseconds to attempt looking for a suitable network
            /// before {@link android.net.ConnectivityManager.NetworkCallback#onUnavailable() NetworkCallback#onUnavailable()} is called. The timeout must
            /// be a positive value (i.e. >0).
            #[allow(non_snake_case)]
            fn request_network3_int {
                name = "requestNetwork",
                sig = (request: "android.net.NetworkRequest", network_callback: crate::android::net::ConnectivityManagerNetworkCallback, timeout_ms: jint),
            },
            /// Request a network to satisfy a set of {@link android.net.NetworkCapabilities NetworkCapabilities}, limited
            /// by a timeout.
            ///
            /// This method behaves identically to
            /// {@link #requestNetwork(android.net.NetworkRequest,android.net.ConnectivityManager.NetworkCallback,int)} but runs all the callbacks
            /// on the passed Handler.
            ///
            /// This method has the same permission requirements as
            /// {@link #requestNetwork(android.net.NetworkRequest,android.net.ConnectivityManager.NetworkCallback)}, is subject to the same limitations,
            /// and throws the same exceptions in the same conditions.
            ///
            ///
            /// # Parameters
            /// * `request` - {@link android.net.NetworkRequest NetworkRequest} describing this request.
            /// * `network_callback` - The {@link android.net.ConnectivityManager.NetworkCallback NetworkCallback} to be utilized for this request. Note
            /// the callback must not be shared - it uniquely specifies this request.
            ///
            /// # Parameters
            /// * `handler` - {@link android.os.Handler Handler} to specify the thread upon which the callback will be invoked.
            /// * `timeout_ms` - The time in milliseconds to attempt looking for a suitable network
            /// before {@link android.net.ConnectivityManager.NetworkCallback#onUnavailable NetworkCallback#onUnavailable} is called.
            fn request_network {
                name = "requestNetwork",
                sig = (request: "android.net.NetworkRequest", network_callback: crate::android::net::ConnectivityManagerNetworkCallback, handler: "android.os.Handler", timeout_ms: jint),
            },
            /// Specifies the preferred network type.  When the device has more
            /// than one type available the preferred network type will be used.
            ///
            ///
            /// # Parameters
            /// * `preference` - the network type to prefer over all others.  It is
            /// unspecified what happens to the old preferred network in the
            /// overall ordering.
            /// with many more than two networks - we'd need an array to express
            /// preference.  Instead we use dynamic network properties of
            /// the networks to describe their precedence.
            #[deprecated]
            fn set_network_preference(preference: jint),
            /// Binds the current process to {@code network}.  All Sockets created in the future
            /// (and not explicitly bound via a bound SocketFactory from
            /// {@link android.net.Network#getSocketFactory() Network.getSocketFactory()}) will be bound to
            /// {@code network}.  All host name resolutions will be limited to {@code network} as well.
            /// Note that if {@code network} ever disconnects, all Sockets created in this way will cease to
            /// work and all host name resolutions will fail.  This is by design so an application doesn't
            /// accidentally use Sockets it thinks are still bound to a particular {@link android.net.Network Network}.
            /// To clear binding pass {@code null} for {@code network}.  Using individually bound
            /// Sockets created by Network.getSocketFactory().createSocket() and
            /// performing network-specific host name resolutions via
            /// {@link android.net.Network#getAllByName Network.getAllByName} is preferred to calling
            /// {@code setProcessDefaultNetwork}.
            ///
            ///
            /// # Parameters
            /// * `network` - The {@link android.net.Network Network} to bind the current process to, or {@code null} to clear
            /// the current binding.
            ///
            /// # Returns
            /// {@code true} on success, {@code false} if the {@link android.net.Network Network} is no longer valid.
            /// {@link #bindProcessToNetwork} instead.  {@code bindProcessToNetwork}
            /// is a direct replacement.
            #[deprecated]
            static fn set_process_default_network(network: crate::android::net::Network) -> jboolean,
            /// Unregisters a callback previously registered via
            /// {@link #registerNetworkCallback(android.net.NetworkRequest,android.app.PendingIntent)}.
            ///
            ///
            /// # Parameters
            /// * `operation` - A PendingIntent equal (as defined by {@link android.content.Intent#filterEquals Intent#filterEquals}) to the
            /// PendingIntent passed to
            /// {@link #registerNetworkCallback(android.net.NetworkRequest,android.app.PendingIntent)}.
            /// Cannot be null.
            fn unregister_network_callback_pending_intent {
                name = "unregisterNetworkCallback",
                sig = (operation: "android.app.PendingIntent"),
            },
            /// Unregisters a {@code NetworkCallback} and possibly releases networks originating from
            /// {@link #requestNetwork(android.net.NetworkRequest,android.net.ConnectivityManager.NetworkCallback)} and
            /// {@link #registerNetworkCallback(android.net.NetworkRequest,android.net.ConnectivityManager.NetworkCallback)} calls.
            /// If the given {@code NetworkCallback} had previously been used with
            /// {@code #requestNetwork}, any networks that had been connected to only to satisfy that request
            /// will be disconnected.
            ///
            /// Notifications that would have triggered that {@code NetworkCallback} will immediately stop
            /// triggering it as soon as this call returns.
            ///
            ///
            /// # Parameters
            /// * `network_callback` - The {@link android.net.ConnectivityManager.NetworkCallback NetworkCallback} used when making the request.
            fn unregister_network_callback_connectivity_manager_network_callback {
                name = "unregisterNetworkCallback",
                sig = (network_callback: crate::android::net::ConnectivityManagerNetworkCallback),
            },
        },
    }

    bind_java_type! {
        /// Callback for use with {@link android.net.ConnectivityManager#addDefaultNetworkActiveListener ConnectivityManager#addDefaultNetworkActiveListener}
        /// to find out when the system default network has gone in to a high power state.
        pub ConnectivityManagerOnNetworkActiveListener => "android.net.ConnectivityManager$OnNetworkActiveListener",
        methods {
            /// Called on the main thread of the process to report that the current data network
            /// has become active, and it is now a good time to perform any pending network
            /// operations.  Note that this listener only tells you when the network becomes
            /// active; if at any other time you want to know whether it is active (and thus okay
            /// to initiate network traffic), you can retrieve its instantaneous state with
            /// {@link android.net.ConnectivityManager#isDefaultNetworkActive ConnectivityManager#isDefaultNetworkActive}.
            fn on_network_active(),
        },
    }

    bind_java_type! {
        /// Representation of the capabilities of an active network. Instances are
        /// typically obtained through
        /// {@link android.net.ConnectivityManager.NetworkCallback#onCapabilitiesChanged(android.net.Network,android.net.NetworkCapabilities) NetworkCallback#onCapabilitiesChanged(Network, NetworkCapabilities)}
        /// or {@link android.net.ConnectivityManager#getNetworkCapabilities(android.net.Network) ConnectivityManager#getNetworkCapabilities(Network)}.
        /// 
        /// This replaces the old {@link android.net.ConnectivityManager#TYPE_MOBILE ConnectivityManager#TYPE_MOBILE} method of
        /// network selection. Rather than indicate a need for Wi-Fi because an
        /// application needs high bandwidth and risk obsolescence when a new, fast
        /// network appears (like LTE), the application should specify it needs high
        /// bandwidth. Similarly if an application needs an unmetered network for a bulk
        /// transfer it can specify that rather than assuming all cellular based
        /// connections are metered and all Wi-Fi based connections are not.
        pub NetworkCapabilities => "android.net.NetworkCapabilities",
        type_map = {
            crate::android::net::NetworkCapabilities => "android.net.NetworkCapabilities",
        },
        constructors {
            fn new(),
            fn new_network_capabilities(nc: crate::android::net::NetworkCapabilities),
        },
        fields {
            #[allow(non_snake_case)]
            static CREATOR {
                sig = "android.os.Parcelable$Creator",
                get = CREATOR,
            },
            /// Indicates that this network was found to have a captive portal in place last time it was
            /// probed.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_CAPTIVE_PORTAL {
                sig = jint,
                get = NET_CAPABILITY_CAPTIVE_PORTAL,
            },
            /// Indicates this is a network that has the ability to reach the carrier's
            /// CBS servers, used for carrier specific services.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_CBS {
                sig = jint,
                get = NET_CAPABILITY_CBS,
            },
            /// Indicates this is a network that has the ability to reach the carrier's
            /// DUN or tethering gateway.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_DUN {
                sig = jint,
                get = NET_CAPABILITY_DUN,
            },
            /// Indicates this is a network that has the ability to reach a carrier's
            /// Emergency IMS servers or other services, used for network signaling
            /// during emergency calls.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_EIMS {
                sig = jint,
                get = NET_CAPABILITY_EIMS,
            },
            /// Indicates that this network is intended for enterprise use.
            /// 
            /// 5G URSP rules may indicate that all data should use a connection dedicated for enterprise
            /// use. If the enterprise capability is requested, all enterprise traffic will be routed over
            /// the connection with this capability.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_ENTERPRISE {
                sig = jint,
                get = NET_CAPABILITY_ENTERPRISE,
            },
            /// Indicates that this network is available for use by apps, and not a network that is being
            /// kept up in the background to facilitate fast network switching.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_FOREGROUND {
                sig = jint,
                get = NET_CAPABILITY_FOREGROUND,
            },
            /// Indicates this is a network that has the ability to reach the carrier's
            /// FOTA portal, used for over the air updates.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_FOTA {
                sig = jint,
                get = NET_CAPABILITY_FOTA,
            },
            /// Indicates that this network is connected to an automotive head unit.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_HEAD_UNIT {
                sig = jint,
                get = NET_CAPABILITY_HEAD_UNIT,
            },
            /// Indicates this is a network that has the ability to reach a carrier's
            /// Initial Attach servers.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_IA {
                sig = jint,
                get = NET_CAPABILITY_IA,
            },
            /// Indicates this is a network that has the ability to reach the carrier's
            /// IMS servers, used for network registration and signaling.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_IMS {
                sig = jint,
                get = NET_CAPABILITY_IMS,
            },
            /// Indicates that this network should be able to reach the internet.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_INTERNET {
                sig = jint,
                get = NET_CAPABILITY_INTERNET,
            },
            /// Indicates this is a network that has the ability to reach a carrier's Mission Critical
            /// servers.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_MCX {
                sig = jint,
                get = NET_CAPABILITY_MCX,
            },
            /// Indicates this is a network that has the ability to reach the
            /// carrier's MMSC for sending and receiving MMS messages.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_MMS {
                sig = jint,
                get = NET_CAPABILITY_MMS,
            },
            /// Indicates that this network is not congested.
            /// 
            /// When a network is congested, applications should defer network traffic
            /// that can be done at a later time, such as uploading analytics.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_NOT_CONGESTED {
                sig = jint,
                get = NET_CAPABILITY_NOT_CONGESTED,
            },
            /// Indicates that this network is unmetered.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_NOT_METERED {
                sig = jint,
                get = NET_CAPABILITY_NOT_METERED,
            },
            /// Indicates that this network is available for general use.  If this is not set
            /// applications should not attempt to communicate on this network.  Note that this
            /// is simply informative and not enforcement - enforcement is handled via other means.
            /// Set by default.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_NOT_RESTRICTED {
                sig = jint,
                get = NET_CAPABILITY_NOT_RESTRICTED,
            },
            /// Indicates that this network is not roaming.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_NOT_ROAMING {
                sig = jint,
                get = NET_CAPABILITY_NOT_ROAMING,
            },
            /// Indicates that this network is not currently suspended.
            /// 
            /// When a network is suspended, the network's IP addresses and any connections
            /// established on the network remain valid, but the network is temporarily unable
            /// to transfer data. This can happen, for example, if a cellular network experiences
            /// a temporary loss of signal, such as when driving through a tunnel, etc.
            /// A network with this capability is not suspended, so is expected to be able to
            /// transfer data.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_NOT_SUSPENDED {
                sig = jint,
                get = NET_CAPABILITY_NOT_SUSPENDED,
            },
            /// Indicates that this network is not a VPN.  This capability is set by default and should be
            /// explicitly cleared for VPN networks.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_NOT_VPN {
                sig = jint,
                get = NET_CAPABILITY_NOT_VPN,
            },
            /// Indicates this is a network that has the ability to reach a carrier's
            /// RCS servers, used for Rich Communication Services.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_RCS {
                sig = jint,
                get = NET_CAPABILITY_RCS,
            },
            /// Indicates this is a network that has the ability to reach the carrier's
            /// SUPL server, used to retrieve GPS information.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_SUPL {
                sig = jint,
                get = NET_CAPABILITY_SUPL,
            },
            /// Indicates that this network is temporarily unmetered.
            /// 
            /// This capability will be set for networks that are generally metered, but are currently
            /// unmetered, e.g., because the user is in a particular area. This capability can be changed at
            /// any time. When it is removed, applications are responsible for stopping any data transfer
            /// that should not occur on a metered network.
            /// Note that most apps should use {@link #NET_CAPABILITY_NOT_METERED} instead. For more
            /// information, see https://developer.android.com/about/versions/11/features/5g#meteredness.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_TEMPORARILY_NOT_METERED {
                sig = jint,
                get = NET_CAPABILITY_TEMPORARILY_NOT_METERED,
            },
            /// Indicates that the user has indicated implicit trust of this network.  This
            /// generally means it's a sim-selected carrier, a plugged in ethernet, a paired
            /// BT device or a wifi the user asked to connect to.  Untrusted networks
            /// are probably limited to unknown wifi AP.  Set by default.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_TRUSTED {
                sig = jint,
                get = NET_CAPABILITY_TRUSTED,
            },
            /// Indicates that connectivity on this network was successfully validated. For example, for a
            /// network with NET_CAPABILITY_INTERNET, it means that Internet connectivity was successfully
            /// detected.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_VALIDATED {
                sig = jint,
                get = NET_CAPABILITY_VALIDATED,
            },
            /// Indicates this is a network that has the ability to reach a Wi-Fi direct
            /// peer.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_WIFI_P2P {
                sig = jint,
                get = NET_CAPABILITY_WIFI_P2P,
            },
            /// Indicates this is a network that has the ability to reach a carrier's
            /// XCAP servers, used for configuration and control.
            #[allow(non_snake_case)]
            static NET_CAPABILITY_XCAP {
                sig = jint,
                get = NET_CAPABILITY_XCAP,
            },
            /// Magic value that indicates no signal strength provided. A request specifying this value is
            /// always satisfied.
            #[allow(non_snake_case)]
            static SIGNAL_STRENGTH_UNSPECIFIED {
                sig = jint,
                get = SIGNAL_STRENGTH_UNSPECIFIED,
            },
            /// Indicates this network uses a Bluetooth transport.
            #[allow(non_snake_case)]
            static TRANSPORT_BLUETOOTH {
                sig = jint,
                get = TRANSPORT_BLUETOOTH,
            },
            /// Indicates this network uses a Cellular transport.
            #[allow(non_snake_case)]
            static TRANSPORT_CELLULAR {
                sig = jint,
                get = TRANSPORT_CELLULAR,
            },
            /// Indicates this network uses an Ethernet transport.
            #[allow(non_snake_case)]
            static TRANSPORT_ETHERNET {
                sig = jint,
                get = TRANSPORT_ETHERNET,
            },
            /// Indicates this network uses a LoWPAN transport.
            #[allow(non_snake_case)]
            static TRANSPORT_LOWPAN {
                sig = jint,
                get = TRANSPORT_LOWPAN,
            },
            /// Indicates this network uses a USB transport.
            #[allow(non_snake_case)]
            static TRANSPORT_USB {
                sig = jint,
                get = TRANSPORT_USB,
            },
            /// Indicates this network uses a VPN transport.
            #[allow(non_snake_case)]
            static TRANSPORT_VPN {
                sig = jint,
                get = TRANSPORT_VPN,
            },
            /// Indicates this network uses a Wi-Fi transport.
            #[allow(non_snake_case)]
            static TRANSPORT_WIFI {
                sig = jint,
                get = TRANSPORT_WIFI,
            },
            /// Indicates this network uses a Wi-Fi Aware transport.
            #[allow(non_snake_case)]
            static TRANSPORT_WIFI_AWARE {
                sig = jint,
                get = TRANSPORT_WIFI_AWARE,
            },
        },
        methods {
            fn describe_contents() -> jint,
            fn equals(obj: JObject) -> jboolean,
            /// Gets all the capabilities set on this {@code NetworkCapability} instance.
            ///
            ///
            /// # Returns
            /// an array of capability values for this instance.
            fn get_capabilities() -> jint[],
            /// Retrieves the downstream bandwidth for this network in Kbps.  This always only refers to
            /// the estimated first hop transport bandwidth.
            ///
            ///
            /// # Returns
            /// The estimated first hop downstream (network to device) bandwidth.
            fn get_link_downstream_bandwidth_kbps() -> jint,
            /// Retrieves the upstream bandwidth for this network in Kbps.  This always only refers to
            /// the estimated first hop transport bandwidth.
            ///
            ///
            /// # Returns
            /// The estimated first hop upstream (device to network) bandwidth.
            fn get_link_upstream_bandwidth_kbps() -> jint,
            /// Gets the optional bearer specific network specifier. May be {@code null} if not set.
            ///
            ///
            /// # Returns
            /// The optional {@link android.net.NetworkSpecifier NetworkSpecifier} specifying the bearer specific network
            /// specifier or {@code null}.
            fn get_network_specifier() -> "android.net.NetworkSpecifier",
            /// Retrieves the UID of the app that owns this network.
            ///
            /// For user privacy reasons, this field will only be populated if the following conditions
            /// are met:
            ///
            /// The caller is the network owner, AND one of the following sets of requirements is met:
            ///
            /// <ol>
            /// <li>The described Network is a VPN
            /// </ol>
            ///
            /// OR:
            ///
            /// <ol>
            /// <li>The calling app is the network owner
            /// <li>The calling app has the ACCESS_FINE_LOCATION permission granted
            /// <li>The user's location toggle is on
            /// </ol>
            ///
            /// Instances of NetworkCapabilities sent to apps without the appropriate permissions will have
            /// this field cleared out.
            ///
            /// 
            /// This field will only be populated for VPN and wifi network suggestor apps (i.e using
            /// {@link WifiNetworkSuggestion}), and only for the network they own.
            /// In the case of wifi network suggestors apps, this field is also location sensitive, so the
            /// app needs to hold {@link android.Manifest.permission#ACCESS_FINE_LOCATION} permission. If the
            /// app targets SDK version greater than or equal to {@link android.os.Build.VERSION_CODES#S Build.VERSION_CODES#S}, then they
            /// also need to use {@link android.net.ConnectivityManager.NetworkCallback#FLAG_INCLUDE_LOCATION_INFO NetworkCallback#FLAG_INCLUDE_LOCATION_INFO} to get the info in their
            /// callback. If the apps targets SDK version equal to {{@link android.os.Build.VERSION_CODES#R Build.VERSION_CODES#R}, this field
            /// will always be included. The app will be blamed for location access if this field is
            /// included.
            /// 
            fn get_owner_uid() -> jint,
            /// Retrieves the signal strength.
            ///
            ///
            /// # Returns
            /// The bearer-specific signal strength.
            fn get_signal_strength() -> jint,
            /// Returns a transport-specific information container. The application may cast this
            /// container to a concrete sub-class based on its knowledge of the network request. The
            /// application should be able to deal with a {@code null} return value or an invalid case,
            /// e.g. use {@code instanceof} operator to verify expected type.
            ///
            ///
            /// # Returns
            /// A concrete implementation of the {@link android.net.TransportInfo TransportInfo} class or null if not
            /// available for the network.
            fn get_transport_info() -> "android.net.TransportInfo",
            /// Tests for the presence of a capability on this instance.
            ///
            ///
            /// # Parameters
            /// * `capability` - the capabilities to be tested for.
            ///
            /// # Returns
            /// {@code true} if set on this instance.
            fn has_capability(capability: jint) -> jboolean,
            /// Tests for the presence of a transport on this instance.
            ///
            ///
            /// # Parameters
            /// * `transport_type` - the transport type to be tested for.
            ///
            /// # Returns
            /// {@code true} if set on this instance.
            fn has_transport(transport_type: jint) -> jboolean,
            fn hash_code() -> jint,
            fn to_string() -> JString,
            fn write_to_parcel(dest: "android.os.Parcel", flags: jint),
        },
    }

    bind_java_type! {
        /// Base class for {@code NetworkRequest} callbacks. Used for notifications about network
        /// changes. Should be extended by applications wanting notifications.
        ///
        /// A {@code NetworkCallback} is registered by calling
        /// {@link #requestNetwork(android.net.NetworkRequest,android.net.ConnectivityManager.NetworkCallback)},
        /// {@link #registerNetworkCallback(android.net.NetworkRequest,android.net.ConnectivityManager.NetworkCallback)},
        /// or {@link #registerDefaultNetworkCallback(android.net.ConnectivityManager.NetworkCallback)}. A {@code NetworkCallback} is
        /// unregistered by calling {@link #unregisterNetworkCallback(android.net.ConnectivityManager.NetworkCallback)}.
        /// A {@code NetworkCallback} should be registered at most once at any time.
        /// A {@code NetworkCallback} that has been unregistered can be registered again.
        pub ConnectivityManagerNetworkCallback => "android.net.ConnectivityManager$NetworkCallback",
        type_map = {
            crate::android::net::LinkProperties => "android.net.LinkProperties",
            crate::android::net::Network => "android.net.Network",
            crate::android::net::NetworkCapabilities => "android.net.NetworkCapabilities",
        },
        constructors {
            fn new(),
            fn new_int(flags: jint),
        },
        fields {
            /// Use this flag to include any location sensitive data in {@link android.net.NetworkCapabilities NetworkCapabilities} sent
            /// via {@link #onCapabilitiesChanged(android.net.Network,android.net.NetworkCapabilities)}.
            /// 
            /// These include:
            /// <li> Some transport info instances (retrieved via
            /// {@link android.net.NetworkCapabilities#getTransportInfo() NetworkCapabilities#getTransportInfo()}) like {@link android.net.wifi.WifiInfo}
            /// contain location sensitive information.
            /// <li> OwnerUid (retrieved via {@link android.net.NetworkCapabilities#getOwnerUid() NetworkCapabilities#getOwnerUid()} is location
            /// sensitive for wifi suggestor apps (i.e using {@link WifiNetworkSuggestion}).</li>
            /// 
            /// 
            /// Note:
            /// <li> Retrieving this location sensitive information (subject to app's location
            /// permissions) will be noted by system. </li>
            /// <li> Without this flag any {@link android.net.NetworkCapabilities NetworkCapabilities} provided via the callback does
            /// not include location sensitive info.
            /// 
            #[allow(non_snake_case)]
            static FLAG_INCLUDE_LOCATION_INFO {
                sig = jint,
                get = FLAG_INCLUDE_LOCATION_INFO,
            },
        },
        methods {
            /// Called when the framework connects and has declared a new network ready for use.
            ///
            /// For callbacks registered with {@link #registerNetworkCallback}, multiple networks may
            /// be available at the same time, and onAvailable will be called for each of these as they
            /// appear.
            ///
            /// For callbacks registered with {@link #requestNetwork} and
            /// {@link #registerDefaultNetworkCallback}, this means the network passed as an argument
            /// is the new best network for this request and is now tracked by this callback ; this
            /// callback will no longer receive method calls about other networks that may have been
            /// passed to this method previously. The previously-best network may have disconnected, or
            /// it may still be around and the newly-best network may simply be better.
            ///
            /// Starting with {@link android.os.Build.VERSION_CODES#O}, this will always immediately
            /// be followed by a call to {@link #onCapabilitiesChanged(android.net.Network,android.net.NetworkCapabilities)}
            /// then by a call to {@link #onLinkPropertiesChanged(android.net.Network,android.net.LinkProperties)}, and a call
            /// to {@link #onBlockedStatusChanged(android.net.Network,boolean)}.
            ///
            /// Do NOT call {@link #getNetworkCapabilities(android.net.Network)} or
            /// {@link #getLinkProperties(android.net.Network)} or other synchronous ConnectivityManager methods in
            /// this callback as this is prone to race conditions (there is no guarantee the objects
            /// returned by these methods will be current). Instead, wait for a call to
            /// {@link #onCapabilitiesChanged(android.net.Network,android.net.NetworkCapabilities)} and
            /// {@link #onLinkPropertiesChanged(android.net.Network,android.net.LinkProperties)} whose arguments are guaranteed
            /// to be well-ordered with respect to other callbacks.
            ///
            ///
            /// # Parameters
            /// * `network` - The {@link android.net.Network Network} of the satisfying network.
            fn on_available(network: crate::android::net::Network),
            /// Called when access to the specified network is blocked or unblocked.
            ///
            /// Do NOT call {@link #getNetworkCapabilities(android.net.Network)} or
            /// {@link #getLinkProperties(android.net.Network)} or other synchronous ConnectivityManager methods in
            /// this callback as this is prone to race conditions : calling these methods while in a
            /// callback may return an outdated or even a null object.
            ///
            ///
            /// # Parameters
            /// * `network` - The {@link android.net.Network Network} whose blocked status has changed.
            /// * `blocked` - The blocked status of this {@link android.net.Network Network}.
            fn on_blocked_status_changed(network: crate::android::net::Network, blocked: jboolean),
            /// Called when the network corresponding to this request changes capabilities but still
            /// satisfies the requested criteria.
            ///
            /// Starting with {@link android.os.Build.VERSION_CODES#O} this method is guaranteed
            /// to be called immediately after {@link #onAvailable}.
            ///
            /// Do NOT call {@link #getLinkProperties(android.net.Network)} or other synchronous
            /// ConnectivityManager methods in this callback as this is prone to race conditions :
            /// calling these methods while in a callback may return an outdated or even a null object.
            ///
            ///
            /// # Parameters
            /// * `network` - The {@link android.net.Network Network} whose capabilities have changed.
            /// * `network_capabilities` - The new {@link android.net.NetworkCapabilities NetworkCapabilities} for this
            /// network.
            fn on_capabilities_changed(network: crate::android::net::Network, network_capabilities: crate::android::net::NetworkCapabilities),
            /// Called when the network corresponding to this request changes {@link android.net.LinkProperties LinkProperties}.
            ///
            /// Starting with {@link android.os.Build.VERSION_CODES#O} this method is guaranteed
            /// to be called immediately after {@link #onAvailable}.
            ///
            /// Do NOT call {@link #getNetworkCapabilities(android.net.Network)} or other synchronous
            /// ConnectivityManager methods in this callback as this is prone to race conditions :
            /// calling these methods while in a callback may return an outdated or even a null object.
            ///
            ///
            /// # Parameters
            /// * `network` - The {@link android.net.Network Network} whose link properties have changed.
            /// * `link_properties` - The new {@link android.net.LinkProperties LinkProperties} for this network.
            fn on_link_properties_changed(network: crate::android::net::Network, link_properties: crate::android::net::LinkProperties),
            /// Called when the network is about to be lost, typically because there are no outstanding
            /// requests left for it. This may be paired with a {@link android.net.ConnectivityManager.NetworkCallback#onAvailable NetworkCallback#onAvailable} call
            /// with the new replacement network for graceful handover. This method is not guaranteed
            /// to be called before {@link android.net.ConnectivityManager.NetworkCallback#onLost NetworkCallback#onLost} is called, for example in case a
            /// network is suddenly disconnected.
            ///
            /// Do NOT call {@link #getNetworkCapabilities(android.net.Network)} or
            /// {@link #getLinkProperties(android.net.Network)} or other synchronous ConnectivityManager methods in
            /// this callback as this is prone to race conditions ; calling these methods while in a
            /// callback may return an outdated or even a null object.
            ///
            ///
            /// # Parameters
            /// * `network` - The {@link android.net.Network Network} that is about to be lost.
            /// * `max_ms_to_live` - The time in milliseconds the system intends to keep the network
            /// connected for graceful handover; note that the network may still
            /// suffer a hard loss at any time.
            fn on_losing(network: crate::android::net::Network, max_ms_to_live: jint),
            /// Called when a network disconnects or otherwise no longer satisfies this request or
            /// callback.
            ///
            /// If the callback was registered with requestNetwork() or
            /// registerDefaultNetworkCallback(), it will only be invoked against the last network
            /// returned by onAvailable() when that network is lost and no other network satisfies
            /// the criteria of the request.
            ///
            /// If the callback was registered with registerNetworkCallback() it will be called for
            /// each network which no longer satisfies the criteria of the callback.
            ///
            /// Do NOT call {@link #getNetworkCapabilities(android.net.Network)} or
            /// {@link #getLinkProperties(android.net.Network)} or other synchronous ConnectivityManager methods in
            /// this callback as this is prone to race conditions ; calling these methods while in a
            /// callback may return an outdated or even a null object.
            ///
            ///
            /// # Parameters
            /// * `network` - The {@link android.net.Network Network} lost.
            fn on_lost(network: crate::android::net::Network),
            /// Called if no network is found within the timeout time specified in
            /// {@link #requestNetwork(android.net.NetworkRequest,android.net.ConnectivityManager.NetworkCallback,int)} call or if the
            /// requested network request cannot be fulfilled (whether or not a timeout was
            /// specified). When this callback is invoked the associated
            /// {@link android.net.NetworkRequest NetworkRequest} will have already been removed and released, as if
            /// {@link #unregisterNetworkCallback(android.net.ConnectivityManager.NetworkCallback)} had been called.
            fn on_unavailable(),
        },
    }


    /// Initialize all Java bindings in this module.
    ///
    /// This loads and caches JClass references and method/field IDs for all
    /// bindings in this module and its child modules.
    ///
    /// # Arguments
    ///
    /// * `env` - The JNI environment
    /// * `loader` - The LoaderContext to use for loading classes
    pub fn jni_init(env: &::jni::Env, loader: &::jni::refs::LoaderContext) -> ::jni::errors::Result<()> {
        let _ = NetworkAPI::get(env, loader)?;
        let _ = LinkPropertiesAPI::get(env, loader)?;
        let _ = ConnectivityManagerAPI::get(env, loader)?;
        let _ = ConnectivityManagerOnNetworkActiveListenerAPI::get(env, loader)?;
        let _ = NetworkCapabilitiesAPI::get(env, loader)?;
        let _ = ConnectivityManagerNetworkCallbackAPI::get(env, loader)?;
        Ok(())
    }
}

    /// Initialize all Java bindings in this module.
    ///
    /// This loads and caches JClass references and method/field IDs for all
    /// bindings in this module and its child modules.
    ///
    /// # Arguments
    ///
    /// * `env` - The JNI environment
    /// * `loader` - The LoaderContext to use for loading classes
    pub fn jni_init(env: &::jni::Env, loader: &::jni::refs::LoaderContext) -> ::jni::errors::Result<()> {
        net::jni_init(env, loader)?;
        Ok(())
    }
}
pub mod java {
pub mod net {
    use jni::bind_java_type;

    bind_java_type! {
        /// This class represents an Internet Protocol (IP) address.
        ///
        ///  An IP address is either a 32-bit or 128-bit unsigned number
        /// used by IP, a lower-level protocol on which protocols like UDP and
        /// TCP are built. The IP address architecture is defined by <a
        /// href="http://www.ietf.org/rfc/rfc790.txt"><i>RFC&nbsp;790:
        /// Assigned Numbers</i></a>, <a
        /// href="http://www.ietf.org/rfc/rfc1918.txt"> <i>RFC&nbsp;1918:
        /// Address Allocation for Private Internets</i></a>, <a
        /// href="http://www.ietf.org/rfc/rfc2365.txt"><i>RFC&nbsp;2365:
        /// Administratively Scoped IP Multicast</i></a>, and <a
        /// href="http://www.ietf.org/rfc/rfc2373.txt"><i>RFC&nbsp;2373: IP
        /// Version 6 Addressing Architecture</i></a>. An instance of an
        /// InetAddress consists of an IP address and possibly its
        /// corresponding host name (depending on whether it is constructed
        /// with a host name or whether it has already done reverse host name
        /// resolution).
        ///
        /// <h3> Address types </h3>
        ///
        /// <blockquote><table cellspacing=2 summary="Description of unicast and multicast address types">
        /// <tr><th valign=top><i>unicast</i></th>
        /// <td>An identifier for a single interface. A packet sent to
        /// a unicast address is delivered to the interface identified by
        /// that address.
        ///
        ///  The Unspecified Address -- Also called anylocal or wildcard
        /// address. It must never be assigned to any node. It indicates the
        /// absence of an address. One example of its use is as the target of
        /// bind, which allows a server to accept a client connection on any
        /// interface, in case the server host has multiple interfaces.
        ///
        ///  The <i>unspecified</i> address must not be used as
        /// the destination address of an IP packet.
        ///
        ///  The <i>Loopback</i> Addresses -- This is the address
        /// assigned to the loopback interface. Anything sent to this
        /// IP address loops around and becomes IP input on the local
        /// host. This address is often used when testing a
        /// client.</td></tr>
        /// <tr><th valign=top><i>multicast</i></th>
        /// <td>An identifier for a set of interfaces (typically belonging
        /// to different nodes). A packet sent to a multicast address is
        /// delivered to all interfaces identified by that address.</td></tr>
        /// </table></blockquote>
        ///
        /// <h4> IP address scope </h4>
        ///
        ///  <i>Link-local</i> addresses are designed to be used for addressing
        /// on a single link for purposes such as auto-address configuration,
        /// neighbor discovery, or when no routers are present.
        ///
        ///  <i>Site-local</i> addresses are designed to be used for addressing
        /// inside of a site without the need for a global prefix.
        ///
        ///  <i>Global</i> addresses are unique across the internet.
        ///
        /// <h4> Textual representation of IP addresses </h4>
        ///
        /// The textual representation of an IP address is address family specific.
        ///
        /// 
        ///
        /// For IPv4 address format, please refer to <A
        /// HREF="Inet4Address.html#format">Inet4Address#format</A>; For IPv6
        /// address format, please refer to <A
        /// HREF="Inet6Address.html#format">Inet6Address#format</A>.
        ///
        /// <P>There is a <a href="doc-files/net-properties.html#Ipv4IPv6">couple of
        /// System Properties</a> affecting how IPv4 and IPv6 addresses are used.</P>
        ///
        /// <h4> Host Name Resolution </h4>
        ///
        /// Host name-to-IP address <i>resolution</i> is accomplished through
        /// the use of a combination of local machine configuration information
        /// and network naming services such as the Domain Name System (DNS)
        /// and Network Information Service(NIS). The particular naming
        /// services(s) being used is by default the local machine configured
        /// one. For any host name, its corresponding IP address is returned.
        ///
        ///  <i>Reverse name resolution</i> means that for any IP address,
        /// the host associated with the IP address is returned.
        ///
        ///  The InetAddress class provides methods to resolve host names to
        /// their IP addresses and vice versa.
        ///
        /// <h4> InetAddress Caching </h4>
        ///
        /// The InetAddress class has a cache to store successful as well as
        /// unsuccessful host name resolutions.
        ///
        ///  By default, when a security manager is installed, in order to
        /// protect against DNS spoofing attacks,
        /// the result of positive host name resolutions are
        /// cached forever. When a security manager is not installed, the default
        /// behavior is to cache entries for a finite (implementation dependent)
        /// period of time. The result of unsuccessful host
        /// name resolution is cached for a very short period of time (10
        /// seconds) to improve performance.
        ///
        ///  If the default behavior is not desired, then a Java security property
        /// can be set to a different Time-to-live (TTL) value for positive
        /// caching. Likewise, a system admin can configure a different
        /// negative caching TTL value when needed.
        ///
        ///  Two Java security properties control the TTL values used for
        /// positive and negative host name resolution caching:
        ///
        /// <blockquote>
        /// <dl>
        /// <dt><b>networkaddress.cache.ttl</b></dt>
        /// <dd>Indicates the caching policy for successful name lookups from
        /// the name service. The value is specified as as integer to indicate
        /// the number of seconds to cache the successful lookup. The default
        /// setting is to cache for an implementation specific period of time.
        /// 
        /// A value of -1 indicates "cache forever".
        /// </dd>
        /// <dt><b>networkaddress.cache.negative.ttl</b> (default: 10)</dt>
        /// <dd>Indicates the caching policy for un-successful name lookups
        /// from the name service. The value is specified as as integer to
        /// indicate the number of seconds to cache the failure for
        /// un-successful lookups.
        /// 
        /// A value of 0 indicates "never cache".
        /// A value of -1 indicates "cache forever".
        /// </dd>
        /// </dl>
        /// </blockquote>
        ///
        pub InetAddress => "java.net.InetAddress",
        type_map = {
            crate::java::net::InetAddress => "java.net.InetAddress",
        },
        methods {
            /// Compares this object against the specified object.
            /// The result is {@code true} if and only if the argument is
            /// not {@code null} and it represents the same IP address as
            /// this object.
            /// 
            /// Two instances of {@code InetAddress} represent the same IP
            /// address if the length of the byte arrays returned by
            /// {@code getAddress} is the same for both, and each of the
            /// array components is the same for the byte arrays.
            ///
            ///
            /// # Parameters
            /// * `obj` - the object to compare against.
            ///
            /// # Returns
            /// {@code true} if the objects are the same;
            /// {@code false} otherwise.
            fn equals(obj: JObject) -> jboolean,
            /// Returns the raw IP address of this {@code InetAddress}
            /// object. The result is in network byte order: the highest order
            /// byte of the address is in {@code getAddress()[0]}.
            ///
            ///
            /// # Returns
            /// the raw IP address of this object.
            fn get_address() -> jbyte[],
            /// Given the name of a host, returns an array of its IP addresses,
            /// based on the configured name service on the system.
            ///
            ///  The host name can either be a machine name, such as
            /// "{@code java.sun.com}", or a textual representation of its IP
            /// address. If a literal IP address is supplied, only the
            /// validity of the address format is checked.
            ///
            ///  For {@code host} specified in <i>literal IPv6 address</i>,
            /// either the form defined in RFC 2732 or the literal IPv6 address
            /// format defined in RFC 2373 is accepted. A literal IPv6 address may
            /// also be qualified by appending a scoped zone identifier or scope_id.
            /// The syntax and usage of scope_ids is described
            /// <a href="Inet6Address.html#scoped">here</a>.
            ///  If the host is {@code null} then an {@code InetAddress}
            /// representing an address of the loopback interface is returned.
            /// See <a href="http://www.ietf.org/rfc/rfc3330.txt">RFC&nbsp;3330</a>
            /// section&nbsp;2 and <a href="http://www.ietf.org/rfc/rfc2373.txt">RFC&nbsp;2373</a>
            /// section&nbsp;2.5.3. 
            ///
            ///  If there is a security manager and {@code host} is not
            /// null and {@code host.length() } is not equal to zero, the
            /// security manager's
            /// {@code checkConnect} method is called
            /// with the hostname and {@code -1}
            /// as its arguments to see if the operation is allowed.
            ///
            ///
            /// # Parameters
            /// * `host` - the name of the host, or {@code null}.
            ///
            /// # Returns
            /// an array of all the IP addresses for a given host name.
            /// {@code host} could be found, or if a scope_id was specified
            /// for a global IPv6 address.
            /// {@code checkConnect} method doesn't allow the operation.
            ///
            static fn get_all_by_name(host: JString) -> crate::java::net::InetAddress[],
            /// Returns an {@code InetAddress} object given the raw IP address .
            /// The argument is in network byte order: the highest order
            /// byte of the address is in {@code getAddress()[0]}.
            ///
            ///  This method doesn't block, i.e. no reverse name service lookup
            /// is performed.
            ///
            ///  IPv4 address byte array must be 4 bytes long and IPv6 byte array
            /// must be 16 bytes long
            ///
            ///
            /// # Parameters
            /// * `addr` - the raw IP address in network byte order
            ///
            /// # Returns
            /// an InetAddress object created from the raw IP address.
            static fn get_by_address {
                name = "getByAddress",
                sig = (addr: jbyte[]) -> crate::java::net::InetAddress,
            },
            /// Creates an InetAddress based on the provided host name and IP address.
            /// No name service is checked for the validity of the address.
            ///
            ///  The host name can either be a machine name, such as
            /// "{@code java.sun.com}", or a textual representation of its IP
            /// address.
            ///  No validity checking is done on the host name either.
            ///
            ///  If addr specifies an IPv4 address an instance of Inet4Address
            /// will be returned; otherwise, an instance of Inet6Address
            /// will be returned.
            ///
            ///  IPv4 address byte array must be 4 bytes long and IPv6 byte array
            /// must be 16 bytes long
            ///
            ///
            /// # Parameters
            /// * `host` - the specified host
            /// * `addr` - the raw IP address in network byte order
            ///
            /// # Returns
            /// an InetAddress object created from the raw IP address.
            #[allow(non_snake_case)]
            static fn get_by_address2 {
                name = "getByAddress",
                sig = (host: JString, addr: jbyte[]) -> crate::java::net::InetAddress,
            },
            /// Determines the IP address of a host, given the host's name.
            ///
            ///  The host name can either be a machine name, such as
            /// "{@code java.sun.com}", or a textual representation of its
            /// IP address. If a literal IP address is supplied, only the
            /// validity of the address format is checked.
            ///
            ///  For {@code host} specified in literal IPv6 address,
            /// either the form defined in RFC 2732 or the literal IPv6 address
            /// format defined in RFC 2373 is accepted. IPv6 scoped addresses are also
            /// supported. See <a href="Inet6Address.html#scoped">here</a> for a description of IPv6
            /// scoped addresses.
            ///
            ///  If the host is {@code null} then an {@code InetAddress}
            /// representing an address of the loopback interface is returned.
            /// See <a href="http://www.ietf.org/rfc/rfc3330.txt">RFC&nbsp;3330</a>
            /// section&nbsp;2 and <a href="http://www.ietf.org/rfc/rfc2373.txt">RFC&nbsp;2373</a>
            /// section&nbsp;2.5.3. 
            ///
            ///
            /// # Parameters
            /// * `host` - the specified host, or {@code null}.
            ///
            /// # Returns
            /// an IP address for the given host name.
            /// {@code host} could be found, or if a scope_id was specified
            /// for a global IPv6 address.
            /// and its checkConnect method doesn't allow the operation
            static fn get_by_name(host: JString) -> crate::java::net::InetAddress,
            /// Gets the fully qualified domain name for this IP address.
            /// Best effort method, meaning we may not be able to return
            /// the FQDN depending on the underlying system configuration.
            ///
            /// If there is a security manager, this method first
            /// calls its {@code checkConnect} method
            /// with the hostname and {@code -1}
            /// as its arguments to see if the calling code is allowed to know
            /// the hostname for this IP address, i.e., to connect to the host.
            /// If the operation is not allowed, it will return
            /// the textual representation of the IP address.
            ///
            ///
            /// # Returns
            /// the fully qualified domain name for this IP address,
            /// or if the operation is not allowed by the security check,
            /// the textual representation of the IP address.
            ///
            ///
            fn get_canonical_host_name() -> JString,
            /// Returns the IP address string in textual presentation.
            ///
            ///
            /// # Returns
            /// the raw IP address in a string format.
            fn get_host_address() -> JString,
            /// Gets the host name for this IP address.
            ///
            /// If this InetAddress was created with a host name,
            /// this host name will be remembered and returned;
            /// otherwise, a reverse name lookup will be performed
            /// and the result will be returned based on the system
            /// configured name lookup service. If a lookup of the name service
            /// is required, call
            /// {@link #getCanonicalHostName() getCanonicalHostName}.
            ///
            /// If there is a security manager, its
            /// {@code checkConnect} method is first called
            /// with the hostname and {@code -1}
            /// as its arguments to see if the operation is allowed.
            /// If the operation is not allowed, it will return
            /// the textual representation of the IP address.
            ///
            ///
            /// # Returns
            /// the host name for this IP address, or if the operation
            /// is not allowed by the security check, the textual
            /// representation of the IP address.
            ///
            fn get_host_name() -> JString,
            /// Returns the address of the local host. This is achieved by retrieving
            /// the name of the host from the system, then resolving that name into
            /// an {@code InetAddress}.
            ///
            /// <P>Note: The resolved address may be cached for a short period of time.
            /// </P>
            ///
            /// If there is a security manager, its
            /// {@code checkConnect} method is called
            /// with the local host name and {@code -1}
            /// as its arguments to see if the operation is allowed.
            /// If the operation is not allowed, an InetAddress representing
            /// the loopback address is returned.
            ///
            ///
            /// # Returns
            /// the address of the local host.
            /// be resolved into an address.
            ///
            static fn get_local_host() -> crate::java::net::InetAddress,
            /// Returns the loopback address.
            /// 
            /// The InetAddress returned will represent the IPv4
            /// loopback address, 127.0.0.1, or the IPv6 loopback
            /// address, ::1. The IPv4 loopback address returned
            /// is only one of many in the form 127.*.*.*
            ///
            ///
            /// # Returns
            /// the InetAddress loopback instance.
            static fn get_loopback_address() -> crate::java::net::InetAddress,
            /// Returns a hashcode for this IP address.
            ///
            ///
            /// # Returns
            /// a hash code value for this IP address.
            fn hash_code() -> jint,
            /// Utility routine to check if the InetAddress in a wildcard address.
            ///
            /// # Returns
            /// a {@code boolean} indicating if the Inetaddress is
            /// a wildcard address.
            fn is_any_local_address() -> jboolean,
            /// Utility routine to check if the InetAddress is an link local address.
            ///
            ///
            /// # Returns
            /// a {@code boolean} indicating if the InetAddress is
            /// a link local address; or false if address is not a link local unicast address.
            fn is_link_local_address() -> jboolean,
            /// Utility routine to check if the InetAddress is a loopback address.
            ///
            ///
            /// # Returns
            /// a {@code boolean} indicating if the InetAddress is
            /// a loopback address; or false otherwise.
            fn is_loopback_address() -> jboolean,
            /// Utility routine to check if the multicast address has global scope.
            ///
            ///
            /// # Returns
            /// a {@code boolean} indicating if the address has
            /// is a multicast address of global scope, false if it is not
            /// of global scope or it is not a multicast address
            fn is_mcglobal {
                name = "isMCGlobal",
                sig = () -> jboolean,
            },
            /// Utility routine to check if the multicast address has link scope.
            ///
            ///
            /// # Returns
            /// a {@code boolean} indicating if the address has
            /// is a multicast address of link-local scope, false if it is not
            /// of link-local scope or it is not a multicast address
            fn is_mclink_local {
                name = "isMCLinkLocal",
                sig = () -> jboolean,
            },
            /// Utility routine to check if the multicast address has node scope.
            ///
            ///
            /// # Returns
            /// a {@code boolean} indicating if the address has
            /// is a multicast address of node-local scope, false if it is not
            /// of node-local scope or it is not a multicast address
            fn is_mcnode_local {
                name = "isMCNodeLocal",
                sig = () -> jboolean,
            },
            /// Utility routine to check if the multicast address has organization scope.
            ///
            ///
            /// # Returns
            /// a {@code boolean} indicating if the address has
            /// is a multicast address of organization-local scope,
            /// false if it is not of organization-local scope
            /// or it is not a multicast address
            fn is_mcorg_local {
                name = "isMCOrgLocal",
                sig = () -> jboolean,
            },
            /// Utility routine to check if the multicast address has site scope.
            ///
            ///
            /// # Returns
            /// a {@code boolean} indicating if the address has
            /// is a multicast address of site-local scope, false if it is not
            /// of site-local scope or it is not a multicast address
            fn is_mcsite_local {
                name = "isMCSiteLocal",
                sig = () -> jboolean,
            },
            /// Utility routine to check if the InetAddress is an
            /// IP multicast address.
            ///
            /// # Returns
            /// a {@code boolean} indicating if the InetAddress is
            /// an IP multicast address
            fn is_multicast_address() -> jboolean,
            /// Test whether that address is reachable. Best effort is made by the
            /// implementation to try to reach the host, but firewalls and server
            /// configuration may block requests resulting in a unreachable status
            /// while some specific ports may be accessible.
            /// 
            /// Android implementation attempts ICMP ECHO REQUESTs first, on failure it
            /// will fall back to TCP ECHO REQUESTs. Success on either protocol will
            /// return true.
            /// 
            /// The timeout value, in milliseconds, indicates the maximum amount of time
            /// the try should take. If the operation times out before getting an
            /// answer, the host is deemed unreachable. A negative value will result
            /// in an IllegalArgumentException being thrown.
            ///
            ///
            /// # Parameters
            /// * `timeout` - the time, in milliseconds, before the call aborts
            ///
            /// # Returns
            /// a {@code boolean} indicating if the address is reachable.
            fn is_reachable {
                name = "isReachable",
                sig = (timeout: jint) -> jboolean,
            },
            /// Test whether that address is reachable. Best effort is made by the
            /// implementation to try to reach the host, but firewalls and server
            /// configuration may block requests resulting in a unreachable status
            /// while some specific ports may be accessible.
            /// 
            /// Android implementation attempts ICMP ECHO REQUESTs first, on failure it
            /// will fall back to TCP ECHO REQUESTs. Success on either protocol will
            /// return true.
            /// 
            /// The {@code network interface} and {@code ttl} parameters
            /// let the caller specify which network interface the test will go through
            /// and the maximum number of hops the packets should go through.
            /// A negative value for the {@code ttl} will result in an
            /// IllegalArgumentException being thrown.
            /// 
            /// The timeout value, in milliseconds, indicates the maximum amount of time
            /// the try should take. If the operation times out before getting an
            /// answer, the host is deemed unreachable. A negative value will result
            /// in an IllegalArgumentException being thrown.
            ///
            ///
            /// # Parameters
            /// * `netif` - the NetworkInterface through which the
            /// test will be done, or null for any interface
            ///
            /// # Parameters
            /// * `ttl` - the maximum numbers of hops to try or 0 for the
            /// default
            ///
            /// # Parameters
            /// * `timeout` - the time, in milliseconds, before the call aborts
            /// or {@code ttl} are negative.
            ///
            /// # Returns
            /// a {@code boolean}indicating if the address is reachable.
            #[allow(non_snake_case)]
            fn is_reachable3 {
                name = "isReachable",
                sig = (netif: "java.net.NetworkInterface", ttl: jint, timeout: jint) -> jboolean,
            },
            /// Utility routine to check if the InetAddress is a site local address.
            ///
            ///
            /// # Returns
            /// a {@code boolean} indicating if the InetAddress is
            /// a site local address; or false if address is not a site local unicast address.
            fn is_site_local_address() -> jboolean,
            /// Converts this IP address to a {@code String}. The
            /// string returned is of the form: hostname / literal IP
            /// address.
            ///
            /// If the host name is unresolved, no reverse name service lookup
            /// is performed. The hostname part will be represented by an empty string.
            ///
            ///
            /// # Returns
            /// a string representation of this IP address.
            fn to_string() -> JString,
        },
    }


    /// Initialize all Java bindings in this module.
    ///
    /// This loads and caches JClass references and method/field IDs for all
    /// bindings in this module and its child modules.
    ///
    /// # Arguments
    ///
    /// * `env` - The JNI environment
    /// * `loader` - The LoaderContext to use for loading classes
    pub fn jni_init(env: &::jni::Env, loader: &::jni::refs::LoaderContext) -> ::jni::errors::Result<()> {
        let _ = InetAddressAPI::get(env, loader)?;
        Ok(())
    }
}

    /// Initialize all Java bindings in this module.
    ///
    /// This loads and caches JClass references and method/field IDs for all
    /// bindings in this module and its child modules.
    ///
    /// # Arguments
    ///
    /// * `env` - The JNI environment
    /// * `loader` - The LoaderContext to use for loading classes
    pub fn jni_init(env: &::jni::Env, loader: &::jni::refs::LoaderContext) -> ::jni::errors::Result<()> {
        net::jni_init(env, loader)?;
        Ok(())
    }
}

/// Initialize all Java bindings.
///
/// This loads and caches JClass references and method/field IDs for all
/// bindings in all modules.
///
/// # Arguments
///
/// * `env` - The JNI environment
/// * `loader` - The LoaderContext to use for loading classes
pub fn jni_init(env: &jni::Env, loader: &jni::refs::LoaderContext) -> jni::errors::Result<()> {
    android::jni_init(env, loader)?;
    java::jni_init(env, loader)?;
    Ok(())
}
